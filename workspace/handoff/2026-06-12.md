# Handoff — 2026-06-12

## Update - 2026-06-12 session 17 — Polymarket orders UNSTUCK (CLOB V2), Alpaca 422 fixed, bots verified live

**Context:** User set a 9-item roadmap and chose focus = Polymarket orders + bot automation (items
1+3), with "tiny proof orders" risk bounds and explicit approval. The "DNS issues" (item 5a) turned
out to be part of this workstream. Session 16's staged clean-clone batch was committed first
(`c65f0bfa`, user-approved).

**Polymarket order placement — FIXED, proven with a real matched order (`ac21d19a`):**
1. Wrong wallet: `.env` funder pointed at an empty EIP-7702 MetaMask account (sig2,
   `0x0f6A...`); `PROXY_ADDRESS` (`0x8010...`) was an empty EOA with no history. The REAL
   account is the EIP-1167 Polymarket proxy wallet **`0x1e7955f5402c8eb5f2aa7879b36bc8789d8f2091`
   (signatureType=1)** — verified on-chain (unlimited exchange approvals, CTF approvedForAll on
   all 5 exchange contracts) and via data-api (the 3 open positions live there).
   `.env` now: `POLYMARKET_FUNDER_ADDRESS=0x1e7955...`, `POLYMARKET_SIGNATURE_TYPE=1`,
   `PROXY_ADDRESS=0x1e7955...` (local-only file; not committed).
2. **CLOB V2 migration (the actual "stuck" root cause):** Polymarket's CLOB V2 went live
   2026-04-28; the ENTIRE legacy `@polymarket/clob-client` line (even latest 5.8.1) gets
   `{"error":"invalid order version, please use the latest clob-client"}` on every order.
   Migrated `clob_factory.ts` to **`@polymarket/clob-client-v2`** (options-object constructor,
   ethers v5 Wallet still valid as ClobSigner, same method names); legacy package removed.
3. New **`polymarket sell <token> <qty> [price]`** gateway subcommand (submit/preflight
   generalized to side-aware); tick-size override now actually forwarded.
4. **PROOF (user-approved):** SELL 5 @ 0.15 of the ETH-dip-$800 YES token → orderId
   `0x27d7cd70...`, status **matched**; position 10→5 shares; CLOB balance 8,553,025 →
   9,305,985 micro-pUSD (+$0.75 net of the 10% taker fee — markets now carry
   maker/taker_base_fee 1000 bps). Polymarket test bundle 26/26.
   NOTE: balance is Polymarket-internal pUSD ledger, NOT on-chain USDC (chain shows $0) —
   ~$9.31 usable for buys.

**Alpaca 422 — root-caused and fixed (`c385959f`):** `time_in_force` was hardcoded `'gtc'`, but
Alpaca 422-rejects fractional equity orders unless TIF=`day`; platform-style crypto symbols
(`BTCUSDT`) are unknown assets (422). Fixes: TIF rule (crypto→gtc, fractional equity→day, else
gtc), `toAlpacaTradeSymbol()` (BTCUSDT→BTC/USD for known crypto bases), error handler now surfaces
Alpaca's response body. **Proof on paper account:** `buy AAPL 0.5 market --live` → FILLED (day);
`buy BTCUSDT 0.001 market --live` → accepted as BTC/USD. Note: gateway requires `--live` flag,
otherwise orders are dry-run simulated (by design — earlier probes without it never hit Alpaca).

**Bot automation — verified online:**
- Docker is UNWEDGED (daemon healthy; `docker-bot-1` + `docker-web-1` Up 10h). Paper bot cycles
  every 30 min, scans 25 real markets; saturated at 5/5 paper positions so no new fills. Logs
  also show the egress flapping reaching inside the container.
- Edge-trader bot (`bot cycle`): health 6/6 green; full decision engine works end-to-end (scan →
  Truth Machine aiProb → edge calc → would-buy). The 2026-06-02 balance-fetch crash is gone.
  Bot left **enabled=true, liveTrading=false** (dry; only acts when a cycle runs).
  ⚠ Strategy-quality flag: top dry pick was "Epstein suicide note released by May 31?" @0.06
  with claimed 78.9% edge — the deadline already passed; that "edge" is a resolution-lag trap.
  Do NOT enable liveTrading without tightening candidate filtering (e.g., drop markets past
  their stated deadline / low-liquidity).

**"DNS issues" root cause:** NOT DNS. Host-level egress intermittently refuses TCP connects
(`connect EACCES`) to Cloudflare/CLOB/Alpaca IPs, flapping on a seconds timescale — same class as
the 2026-06-07 Docker-registry WSAEACCES. Mitigation landed (`fd15e2e2`): clob-client-v2
`retryOnError` enabled. Remaining: shared retry/backoff helper for raw fetch call sites
(gateway gamma/data-api, polymarket_paper.js, CLI) — queued for the implementation wave.
Likely culprit worth user attention: Windows firewall/AV/VPN connection filtering.

**Commits this session:** `c65f0bfa` (clean-clone wave 1, staged batch), `ac21d19a` (CLOB V2),
`c385959f` (Alpaca 422), `fd15e2e2` (retryOnError).

**User's 9-item roadmap (this session = item 1+3+5a):**
1. ✅ Polymarket orders. 2. TUI revamp (animations/coloring/layout). 3. ✅ bot verification
(live-loop enablement = user decision). 4. Monolith deconstruction. 5. DNS (✅ diagnosed/mitigated)
+ login barrier security hardening. 6. C++ backend verification. 7. RAM optimization.
8. Deep 5-min historical data. 9. Docker deploy + web dashboard revamp (explicitly later).

**Open for next session / user decisions:**
- Enable bot liveTrading? (needs candidate-filter tightening first; positionSizeUsdc=$10 vs $9.31
  balance). Deposit funds for buys? (sells work; buys limited by $9.31 pUSD).
- Items 2/4/6/7/8 to plan as Sonnet implementation waves per lighter-model preference.
- Cancel/keep the two tiny Alpaca paper proof positions (0.5 AAPL filled, 0.001 BTC/USD).
- `polymarket buy/sell` error classifier mislabels CLOB rejections as `invalid_token` (minor).
- `bot health` prints pUSD in micro-units ("9305985.00 pUSD") — cosmetic.

## Update - 2026-06-12 session 17b — Wave 1 delegated fixes landed (4 commits) + UI-trade incident resolved

**Delegation per lighter-model preference:** 3 parallel Sonnet agents with disjoint file ownership;
Fable specced, reviewed diffs (hallucination check), re-ran every gate, committed.

**Landed:**
- `37d2d6d2` fix(api) — GET /api/kill-switch now requires the API token (+contract test, 401
  unauth / non-401 authed; suite pins SOVEREIGN_API_TOKEN before requiring app.js). Audit #1
  CLOSED; app.js gate should lift C→B next audit.
- `32cb5637` fix(gateway) — failed/risk-rejected live orders now ok:false + error + exit 1
  (proven live: fake-symbol probe). clob_rejected error category (no more invalid_token
  mislabels). derive-creds masked by default + --reveal. bot health shows dollars. NEW
  shared/lib/runtime/fetch_retry.js (+7 tests) applied to Gate.io fetch. Audit #2/#4/#5/#6 CLOSED.
- `cafe6eea` fix(bot) — real OrderType.FOK on all bot sell/buy posts + defensive cancel-on-
  unmatched; past-deadline buy guard (verified vs live Gamma: the May-31 trap market would skip).
  Audit #3/#7 CLOSED. Precondition for liveTrading now met on the order-mechanics side.

**Incident (resolved benign):** during gates, 2 untracked Polymarket trades appeared (BUY 13 Yes
@0.09 BTC-ATH filled; BUY 5 No @0.12 BTC-200k open). Investigated every code path (paper engine
has NO order code; bot dry; container runs old SDK + stale env) — then user confirmed they placed
both in the Polymarket UI themselves. Open order cancelled at user request (verified 0 open).
Useful fact: UI and platform share the same derived L2 API key, so the CLOB owner field cannot
distinguish them.

**Wave 2 (queued):** merge submit/preflight duplication; adopt SDK createL2Headers/
updateBalanceAllowance in clob_factory (drop hand-rolled HMAC); retry wrapper for gateway axios
gamma calls + CLI-side fetch sites.

## Update - 2026-06-12 session 17c — Wave 2 landed + C++ backend verified (roadmap item 6)

**Wave 2 (`6875f1fa`, Sonnet-implemented, Fable-reviewed):** submit/preflight deduped behind
_polymarketOrderCore (output shapes pinned + proven); retryTransient() added to fetch_retry and
applied to gamma axios calls + cycle end-date fetch — the EACCES-flap mitigation (task #4) is now
COMPLETE across all polymarket-path HTTP (CLI needed nothing: it has no raw fetches). The
clob_factory createL2Headers adoption was deliberately ABORTED (SDK wants ClobSigner + WebCrypto,
drops UA headers) — hand-rolled L2 builder stays canonical; backlog item removed with rationale.
Full suite after everything: **284/284**.

**C++ backend verification (item 6) — VERIFIED with caveats:**
- Strongest proof: `ml compare` reproduces the Phase-3 parity numbers EXACTLY (bit-level match to
  the Python proof) on all 3 ONNX models, backend onnx_runtime, 19,480 rows. Correlation + risk
  engines respond correctly.
- ctest: 27/29. BOTH failures are fixture-path debt, not logic: ingestion_adapter_test resolves
  config/data_sources.yaml relative to the build dir; kronos_integration_test missing its >=4
  empirical data points. STATE.md's "29/29" claim is stale until fixed.
- Carryover caveat: core test mains are assert-only → no-ops in Release; behavioral anchors are
  the real evidence. Ledger: DEV_REVIEW "Resolutions + C++ verification - 2026-06-12".

**Session 17 totals: 11 commits** (c65f0bfa, ac21d19a, c385959f, fd15e2e2, c4ea88be, abe06311,
37d2d6d2, 32cb5637, cafe6eea, 6875f1fa, + this docs commit). Roadmap items 1, 3, 5a CLOSED;
item 6 verified (2 S-fixes queued); audit findings #1-#7 ALL resolved.

**Next session candidates:** item 2 TUI revamp (big Sonnet wave — spec first), item 4 monolith
deconstruction (trade.js/index.ts/sovereign_cli.js), C++ fixture-path S-fixes, item 7 RAM
profiling, item 8 deep 5-min data research.

## Update - 2026-06-12 session 18 - Polymarket historical archive/backtest mass implementation

**Implemented:** created the repo-local `polymarket-history-backfill` skill and shipped the first
historical research slice. The archive layer now normalizes resolved Gamma markets, writes CLOB
price curves, writes generated point-in-time feature rows, summarizes archive coverage, and keeps
bulk state under ignored `storage/data/polymarket_history/`.

**Command surface:** `polymarket research ingest` and `polymarket history ingest` backfill the
local archive. `polymarket backtest` now prefers archive replay by default, with `--live-fetch` /
`--no-archive` for explicit live fetch and `--repair-missing` for archive repairs. Cost assumptions
are visible through `--fee`, `--half-spread`, `--impact-y`, `--order-notional`, and
`--rolling-market-volume`.

**Policy decision captured:** full PMXT historical order books are not the first archive layer.
Use resolved markets + price curves for initial strategy replay; only use PMXT/order-book-lite for
candidate trade windows after a signal survives replay.

**Verification:** no-network fixture tests pass for archive ingest/replay, feature generation, and
backtest CLI parsing. Focused commands: `node --check` on touched JS/test files; `node --test`
`polymarket_history_archive.test.js`, `polymarket_features.test.js`, and
`polymarket_backtest.test.js`.

## Update - 2026-06-12 session 18b (Claude orchestrator) - Roadmap waves 2/6/7/8 landed via delegated agents; Codex slice reviewed + integrated

**Context:** user authorized standing delegation ("put that into skill" -- now baked into
session-orchestrator Delegation Rule + feedback-lighter-model-implementation memory). 7 Sonnet/Explore
agents ran across the session; Fable specced, reviewed every diff, re-ran every gate, did all commits.

**Commits (6):**
- `e0ad1ff7` fix(core): ctest fixture debt cleared + regime_detector off-by-one (REAL pre-existing
  bug: guard wanted 21 prices, code reads exactly 20; true Debug baseline was 25/29 -- Release NDEBUG
  masked 2 regime failures). ctest -C Debug 29/29 re-verified by Fable; ml compare parity
  bit-identical (xgboost 0.666376 {7061,1275,11144} / logistic 0.468378 / regime 0.456982).
  recovered_live accepted as live mode (snapshot summary + contract test, fixture-based now).
- `d51bfbc1` feat(tui): Phase A revamp per workspace/TUI_REVAMP_SPEC.md -- spinner.js, progress.js,
  SEMANTIC color map, render_helpers.js extraction (frame-identity proven), dynamic page size
  (presets become caps). +44 tests; --json proven 0 ANSI bytes.
- `ac7b10ed` perf(ml): readFamilySources memoization (60s TTL, clearFamilySourcesCache for tests) +
  forwardFillOnto hoisted out of the row loop. Anchor probe 1452MB/41s -> 754MB/3.1s; 4-symbol
  ml dump 21.9s -> 2.8s; output byte-identical (SHA256-proven).
- `508b7d68` docs: TUI_REVAMP_SPEC.md, FIVE_MIN_DATA_SCOPING.md, RAM_PROFILING_REPORT.md.
- `0e90e2a0` feat(polymarket): the concurrent Codex session's archive/features/backtest slice
  (see session 18 entry above), reviewed per codex-review workflow and integrated: tests 28/28,
  point-in-time pins verified, cost model wiring read end-to-end.
- (this close-out docs commit)

**Mid-session discoveries:**
1. User checkpoint commit `76ef48fb` (10:38) landed mid-session and committed the ENTIRE parked
   2026-06-11 batch (+ many untracked files: frontend panels, strategies yaml, config/trading,
   telegram exports). The parked-batch decision is therefore RESOLVED; TUI Phase B is UNBLOCKED.
2. A concurrent Codex session wrote the polymarket slice 13:30-15:43 while waves ran. Treat
   mid-session tree changes as possibly-concurrent-work before assuming agent scope violations.
   CAUTION for future sessions: re-check `git status` + mtimes before staging anything.

**Gates at close: full suite 342/342** (284 baseline +44 TUI +14 polymarket); ctest Debug 29/29
(STATE.md 29/29 claim is TRUE again); graphify-out refresh still deprioritized per user.

**Next-session candidates:** TUI Phase B (status/asset_picker/manifest polish -- now unblocked),
NDJSON streaming for 377MB family JSONs (RAM hotspot #2, M -- needs user format sign-off),
5-min data Phase 1 (Binance crypto, sequential backfill -- FIVE_MIN_DATA_SCOPING.md section 7
decisions pending), monolith deconstruction (roadmap 4), CLI lazy-requires (RAM #5, optional),
Polymarket archive first real ingest run + replay (Codex slice is fixture-tested, not yet run live).

## Update - 2026-06-12 session 19 - Polymarket orderbook-lite phase 2

**Implemented:** added PMXT-based candidate-window snapshots behind `--capture-orderbook-lite`.
Backtests can now write derived order-book rows to `storage/data/polymarket_history/orderbooks-lite/`
with best bid/ask, mid, spread, 1%/5% depth, timestamp, and source.

**Policy:** the feature is opt-in and requires `PMXT_API_KEY` against `https://api.pmxt.dev`.
It only runs after a replay candidate exists, and the test suite uses injected fetchers so it stays
no-network by default.

**Verification:** focused `node --check` passed on the touched JS files, and the focused suite is
green with the new orderbook-lite tests plus the existing archive/features/backtest coverage.

## Update - 2026-06-12 session 20 - Polymarket history backfill corrected, archive populated, session closed

**What changed:** repointed `polymarket history backfill` to the actual local archive ingest path
instead of the PMXT orderbook-lite job, and added `--start-offset` so archive runs can jump past the
newest empty markets and sample older resolved markets with usable price history.

**Result:** the archive path now writes local price history and features under
`storage/data/polymarket_history/`. A deeper batch (`--days 3650 --start-offset 5000 --max-markets 20`)
produced a real archive slice with 20 markets, 20 tokens archived, 20 price points, and 20 feature
rows. The newer top-of-catalog markets were mostly empty by design, so the offset is the practical
way to populate history.

**Separation kept clean:** PMXT orderbook-lite remains a separate opt-in lane and is not the default
meaning of `history backfill`. The archive-first path stays price-history centric; order-book snapshots
remain candidate-window only.

**Verification:** `node --check` passed on `shared/lib/market/polymarket_history.js`,
`backend/cli/commands/trade/trade.js`, and `backend/gateway/src/index.ts`. The live archive run
completed successfully via `node backend/cli/lib/run_trade_gateway.js polymarket history backfill
--days 3650 --start-offset 5000 --max-markets 20 --interval 1h --json`.

## Update - 2026-06-12 session 21 (Claude orchestrator) - mass-implement: Codex slice integrated, TUI Phase B, 5m crypto Phase 1

**Plan:** mass-implement over open carryovers; user selected Batches 1 (TUI Phase B), 2 (S-fixes),
4 (5-min data Phase 1); skipped Batch 3 (NDJSON). Decisions: commit per verified batch; TUI Unicode
rich-gated default-on; 5m depth = 5 years.

**Commits (3):**
- `1f6b5e45` feat(polymarket): sessions 19/20 Codex slice reviewed + integrated (orderbook-lite
  backfill lane, history-backfill repoint, --start-offset). Gates: focused bundle 35/35, tsc clean.
- `b64cf57c` feat(tui): Phase B (cockpit glyphs, asset-picker 60s cache, ? help overlay, manifest
  tuning). Orchestrator fix on review: Ctrl-C falls through the help overlay. TUI surface 99/99;
  status --json 0 ANSI chars.
- `c3fbc3ba` feat(data): 5m crypto deep history Phase 1 (Binance native fetch, sequential
  `crypto-deep-backfill` command, 90d JSON cap at write time, merge-protected sub-daily bins).

**Batch 2 finding:** the C++ indicators default-input S-fix was ALREADY in HEAD (`e0ad1ff7`);
DEV_REVIEW finding #2 (2026-06-12 C++ table) was stale. Verified: probes + ctest -C Debug 29/29.

**5m wave needed a 5-defect correction pass (orchestrator-implemented after the correction agent
hit the Sonnet session limit):** (1) stack overflow spreading the 146k-record merged history;
(2) snapshot-level 90d cap truncated deep fetches before the ts-index; (3) writeTsIndex replace
semantics would let later shallow ingests truncate deep 5m bins (now merge-protected);
(4) --days <= 5 guard; (5) **TwelveData sits before binance in the crypto provider chain and
silently caps history at exactly 5,000 bars** -- first-success break meant deep backfills never
reached Binance; ingestMarketData now supports `options.provider` pinning. Each fix probe-proven
(--days 30 -> 8,640 bars, full-window bin, merge preserved priors).

**Gates at close: full suite 385/385 exit 0** (342 session-18b baseline + 14 crypto-5m + TUI
Phase B additions + Codex-slice tests).

**Running at close:** full 18-symbol 5-year sequential backfill launched in background
(`crypto-deep-backfill --days 1825 --delay-ms 250 --json`); expect ~45-60 min, ~430MB into
storage/data/ts. Re-runs are idempotent (bin merge). Check per-symbol results in its JSON output.

**Carryovers:** CLI lazy-requires (RAM #5, optional) deferred -- agent session limit + file overlap;
NDJSON streaming (RAM #2) skipped by user this session; merge feat/ml-onnx-section -> main still the
user's decision; graphify-out refresh still deprioritized.

## Update - 2026-06-12 session 22 — 5y backfill verified FAILED silently; spread-args root cause fixed; rerun in flight

**Context:** Boot per session-orchestrator; the one flagged carryover was "verify the 18-symbol 5y
5m backfill launched at session 21 close." Verification found it RUNNING (started 21:54:26, PID
6424) but delivering nothing; it finished at ~22:42 with `ok:true, exit 0` and **`bars_5m: 0` for
all 17 real symbols** (only delisted RNDRUSDT reported 969 stale cache bars).

**Root cause (proven, fixed):** `backend/scripts/data_ops/ingest_market_data/index.js:1604`
`snapshot.sources.push(...records)` in the family provider loop. V8 passes spread args on the call
stack; a 5y 5m fetch returns ~525k records → `RangeError: Maximum call stack size exceeded` →
**swallowed by the provider-loop catch** as a generic provider error → symbol resolves to zero
records. Session 21's fix (plain loop at the fetchCryptoSnapshot site, line ~1886) was one level
too deep; its 160k-bar regression test only exercised `fetchCryptoSnapshot`, never the provider
loop. Diagnosis trail: per-symbol `errors:2` (RangeError + "No crypto provider resolved"),
~165s/symbol fetch pace with 8k+ cached Binance responses, every >100k-record symbol at 0 bars,
30d/120d probes fine.

**Fix (uncommitted, awaiting user):**
- `ingest_market_data/index.js`: new `appendRecords()` plain-loop helper (exported); replaced the
  call-spread at the provider loop + 8 sibling record-array sites (equity/commodity aggregation
  at 743/789 would hit the same wall in 5m Phases 2-3; options loop; external quotes; localDerived;
  crypto daily branch). Small bounded pushes (polymarket points, reserves, errors) left as-is.
  Array-literal spreads (`[...a, ...b]`) are iteration-based and safe — not touched.
- `backend/cli/commands/data/data.js`: zero bars + ingest errors now marks the symbol failed
  (results.errors++, exit 1), per-symbol `error` text, `error_messages` in the payload, and
  non-TTY per-symbol completion logging (the old silent-success shape is pinned by a new test).
- `tests/scripts/tests/crypto_5m_backfill.test.js`: stub harness accepts a snapshot factory;
  +2 regression tests (appendRecords 250k no-RangeError; zero-bars-with-errors → ok:false).

**Verification:**
- Focused bundle 16/16; **full suite 387/387 exit 0** (new baseline; was 385).
- Real command end-to-end: `crypto-deep-backfill --days 400 --symbol BTCUSDT` → **115,200 bars
  (exactly 400×288), errors 0, exit 0**; ts-index bin confirms 115,200 bars spanning exactly
  400.0 days. This depth would have RangeError'd pre-fix.

**In flight at write time:** full 18-symbol `--days 1825 --delay-ms 250` rerun relaunched in
background (~60-90 min, ~430MB). Verify per-symbol counts when it completes (BTCUSDT 5y ≈ 525k;
listing-bounded for SUI/PEPE/WIF/POL; RNDRUSDT delisted — expect failure or stale-only).

**Durable lessons:** (1) V8 call-spread limit (~100k args) + a swallowing catch = silent
zero-data ingest; grep for `push(...` whenever arrays can carry deep history. (2) A regression
test at the wrong layer proves nothing about the layer above — the 160k test passed while the
real command failed; always run the REAL command end-to-end at representative scale (re-learned
from session 21, which ran it end-to-end but only at 30 days — also test PAST the cap/limit).
(3) Background-run "ok:true exit 0" is meaningless if the command's failure accounting has gaps.

## Update - 2026-06-12 session 23 - synthetic 5m guard implemented; 1825d rerun still active

**Plan executed:** verify the session-22 5m rerun first, then close the next correctness guard while
the long job continued. Initial ts-index header probe found the rerun was still active, not final:
early symbols had full/native depth, while later symbols were still old. Process confirmed:
PID 14380, command `backend/cli/sovereign_cli.js crypto-deep-backfill --days 1825 --delay-ms 250 --json`,
started 22:46:30. CPU/memory continued moving, so it was not killed or restarted.

**Implemented:** synthetic/daily-derived 5m is now experimental-only at the consumer boundary.
`aggregateCandles` tags future derived rows with `derived_from_timeframe` and `experimental_only`
when lower timeframes are derived from daily-or-above bars. `validateSnapshot` rejects those records
as `synthetic_lower_timeframe`. `shared/lib/ml/dataset.js` filters experimental 5m from JSON and
binary ts-index sources by default, including old non-crypto 5m bins that lack per-record provenance;
`sovereign ml dump` has `--include-experimental-5m` for explicit research opt-in.

**Verification:** syntax checks on touched JS files passed; focused bundle
`node --test tests/scripts/tests/ml_dataset.test.js tests/scripts/strategy_backtest_contract.test.js`
passed 25/25; `node --test tests/scripts/tests/crypto_5m_backfill.test.js` passed 16/16; full
`npm.cmd test` passed **389/389**.

**Carryover:** re-run the ts-index header probe after PID 14380 exits. Last poll before this note:
files were freshly rewritten through NEAR/AVAX/FET/POL, but INJ was still old and RNDR remains the
known delisted/stale edge. Do not mark the full 18-symbol 1825d run complete until the process exits
and the per-symbol counts are captured.

## Update - 2026-06-12 session 23b - other-symbol 5m expansion completed for US equities

**User request:** "next is the 5min data all the way for other symbols."

**Crypto verification closed:** the session-22 1825d crypto process was no longer running. Header
probe over `storage/data/ts/*_5m.bin` found 13 configured crypto symbols at the full 525,506 bars;
SUI/PEPE/WIF/POL/RNDR remain shorter because of listing/provider availability, not because a job is
still in flight.

**Implemented Phase 2:** native 5m for Alpaca-eligible US equities.
- `shared/lib/providers/alpaca.js`: internal timeframe mapping (`5m` -> `5Min`), `next_page_token`
  cursor pagination, default `feed=iex`, `adjustment=split`.
- `shared/lib/data/backfill.js`: 10,000-bar chunks for equities, progress guard, optional
  `chunkDelayMs`.
- `fetchEquityOrIndexSnapshot`: Alpaca sub-daily requests route to native paginated bars and do not
  synthesize missing Alpaca 5m from daily bars.
- CLI: `equity-deep-backfill` with US-only Alpaca plan, explicit non-US skips, `--chunk-delay-ms`,
  and provider-pinned ingest.

**Live run proof:** `node backend/cli/sovereign_cli.js equity-deep-backfill --days 1825
--chunk-delay-ms 500 --json` completed with 33/33 successes, 44 explicit non-US skips, 0 errors,
and 3,100,888 fetched 5m bars. Post-run ts-index verification found 33 Alpaca-backed US-equity bins,
3,101,322 merged rows total, no missing bins (`min=51,993`, `max=112,021`).

**Verification:** affected bundle
`node --test tests/scripts/tests/equity_5m_backfill.test.js tests/scripts/tests/crypto_5m_backfill.test.js
tests/scripts/tests/ml_dataset.test.js tests/scripts/strategy_backtest_contract.test.js` passed 47/47.
Full `npm.cmd test` passed **395/395**. `git diff --check` on touched files passed with LF/CRLF
warnings only.

**Carryover:** Phase 3 indices/commodities/FX still need provider/budget decision or Yahoo 60-day
accumulate-forward. Equity 5m needs a session-gap guard before indicator/backtest consumption. ML
needs explicit 5m cap/performance gates before full-depth 5m feature generation.
