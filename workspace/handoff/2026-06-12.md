# Handoff — 2026-06-12

## Update - 2026-06-12 session 17 — Polymarket orders UNSTUCK (CLOB V2), Alpaca 422 fixed, bots verified live

**Context:** User set a 9-item roadmap and chose focus = Polymarket orders + bot automation (items
1+3), with "tiny proof orders" risk bounds and explicit approval. The "DNS issues" (item 5a) turned
out to be part of this workstream. Session 16's staged clean-clone batch was committed first
(`c65f0bfa`, user-approved).

**Polymarket order placement — FIXED, proven with a real matched order (`ac21d19a`):**
1. Wrong wallet: `.env` funder pointed at an empty EIP-7702 MetaMask account (sig2,
   `0x0f6A...`); `PROXY_ADDRESS` (`0x8010...`) was an empty EOA with no history. The REAL
   account is the EIP-1167 Polymarket proxy wallet **`0x1e7955f5402c8eb5f2aa7879b36bc8789d8f2091`
   (signatureType=1)** — verified on-chain (unlimited exchange approvals, CTF approvedForAll on
   all 5 exchange contracts) and via data-api (the 3 open positions live there).
   `.env` now: `POLYMARKET_FUNDER_ADDRESS=0x1e7955...`, `POLYMARKET_SIGNATURE_TYPE=1`,
   `PROXY_ADDRESS=0x1e7955...` (local-only file; not committed).
2. **CLOB V2 migration (the actual "stuck" root cause):** Polymarket's CLOB V2 went live
   2026-04-28; the ENTIRE legacy `@polymarket/clob-client` line (even latest 5.8.1) gets
   `{"error":"invalid order version, please use the latest clob-client"}` on every order.
   Migrated `clob_factory.ts` to **`@polymarket/clob-client-v2`** (options-object constructor,
   ethers v5 Wallet still valid as ClobSigner, same method names); legacy package removed.
3. New **`polymarket sell <token> <qty> [price]`** gateway subcommand (submit/preflight
   generalized to side-aware); tick-size override now actually forwarded.
4. **PROOF (user-approved):** SELL 5 @ 0.15 of the ETH-dip-$800 YES token → orderId
   `0x27d7cd70...`, status **matched**; position 10→5 shares; CLOB balance 8,553,025 →
   9,305,985 micro-pUSD (+$0.75 net of the 10% taker fee — markets now carry
   maker/taker_base_fee 1000 bps). Polymarket test bundle 26/26.
   NOTE: balance is Polymarket-internal pUSD ledger, NOT on-chain USDC (chain shows $0) —
   ~$9.31 usable for buys.

**Alpaca 422 — root-caused and fixed (`c385959f`):** `time_in_force` was hardcoded `'gtc'`, but
Alpaca 422-rejects fractional equity orders unless TIF=`day`; platform-style crypto symbols
(`BTCUSDT`) are unknown assets (422). Fixes: TIF rule (crypto→gtc, fractional equity→day, else
gtc), `toAlpacaTradeSymbol()` (BTCUSDT→BTC/USD for known crypto bases), error handler now surfaces
Alpaca's response body. **Proof on paper account:** `buy AAPL 0.5 market --live` → FILLED (day);
`buy BTCUSDT 0.001 market --live` → accepted as BTC/USD. Note: gateway requires `--live` flag,
otherwise orders are dry-run simulated (by design — earlier probes without it never hit Alpaca).

**Bot automation — verified online:**
- Docker is UNWEDGED (daemon healthy; `docker-bot-1` + `docker-web-1` Up 10h). Paper bot cycles
  every 30 min, scans 25 real markets; saturated at 5/5 paper positions so no new fills. Logs
  also show the egress flapping reaching inside the container.
- Edge-trader bot (`bot cycle`): health 6/6 green; full decision engine works end-to-end (scan →
  Truth Machine aiProb → edge calc → would-buy). The 2026-06-02 balance-fetch crash is gone.
  Bot left **enabled=true, liveTrading=false** (dry; only acts when a cycle runs).
  ⚠ Strategy-quality flag: top dry pick was "Epstein suicide note released by May 31?" @0.06
  with claimed 78.9% edge — the deadline already passed; that "edge" is a resolution-lag trap.
  Do NOT enable liveTrading without tightening candidate filtering (e.g., drop markets past
  their stated deadline / low-liquidity).

**"DNS issues" root cause:** NOT DNS. Host-level egress intermittently refuses TCP connects
(`connect EACCES`) to Cloudflare/CLOB/Alpaca IPs, flapping on a seconds timescale — same class as
the 2026-06-07 Docker-registry WSAEACCES. Mitigation landed (`fd15e2e2`): clob-client-v2
`retryOnError` enabled. Remaining: shared retry/backoff helper for raw fetch call sites
(gateway gamma/data-api, polymarket_paper.js, CLI) — queued for the implementation wave.
Likely culprit worth user attention: Windows firewall/AV/VPN connection filtering.

**Commits this session:** `c65f0bfa` (clean-clone wave 1, staged batch), `ac21d19a` (CLOB V2),
`c385959f` (Alpaca 422), `fd15e2e2` (retryOnError).

**User's 9-item roadmap (this session = item 1+3+5a):**
1. ✅ Polymarket orders. 2. TUI revamp (animations/coloring/layout). 3. ✅ bot verification
(live-loop enablement = user decision). 4. Monolith deconstruction. 5. DNS (✅ diagnosed/mitigated)
+ login barrier security hardening. 6. C++ backend verification. 7. RAM optimization.
8. Deep 5-min historical data. 9. Docker deploy + web dashboard revamp (explicitly later).

**Open for next session / user decisions:**
- Enable bot liveTrading? (needs candidate-filter tightening first; positionSizeUsdc=$10 vs $9.31
  balance). Deposit funds for buys? (sells work; buys limited by $9.31 pUSD).
- Items 2/4/6/7/8 to plan as Sonnet implementation waves per lighter-model preference.
- Cancel/keep the two tiny Alpaca paper proof positions (0.5 AAPL filled, 0.001 BTC/USD).
- `polymarket buy/sell` error classifier mislabels CLOB rejections as `invalid_token` (minor).
- `bot health` prints pUSD in micro-units ("9305985.00 pUSD") — cosmetic.
