# Session Handoff - 2026-07-29

## Update - 2026-07-29 pending-plan consolidation

The 27 prior plan files are now classified through
`workspace/plans/CURRENT_PENDING_MASTER_PLAN.md`. The master plan is planning-only and is the current
dependency order:

1. M0 exact source, evidence, and continuity closure;
2. M1 credential, dependency, and supply-chain gates;
3. M2 private web-only host qualification;
4. M3 data readiness and point-in-time truth;
5. M4 versioned strategies and comparable replay;
6. M5 canonical monitoring source;
7. M6 paper, writer, recovery, and soak qualification;
8. M7 behavior-preserving maintainability and UX;
9. M8 distribution and optional separate roadmaps.

M0 is the only current action. First inventory the 83-entry dirty worktree into coherent owners/commit groups,
map load-bearing untracked edges, and preflight durable source-evidence/RAG diagnostics. FULL-1/FULL-2 are
closed in the worktree but not in exact `0383d47b`; do not deploy that commit or partially commit the safety
batch.

No implementation, commit, dependency change, credential rotation, remote write, service/container start,
provider poll, canonical-data mutation, bot cycle, webhook, order, public exposure, or live action occurred.

## Update - 2026-07-29 M0 source-evidence repair

M0 reached `GO WITH FIXES -> implemented -> verified -> reviewed`; closure is deferred at the
commit/archive/CI boundary.

- Durable inventory: `workspace/plans/M0_WORKTREE_CHANGESET_INVENTORY.md` covers all 94 current status entries
  and identifies the untracked load-bearing safety owners.
- Source evidence: schema v2, durable ignored local default, atomic active-step checkpoints, bounded sanitized
  diagnostics, and stdout/stderr content fingerprints.
- RAG: per-file process isolation remains, while the redundant inner Node isolation layer is removed so nested
  causes reach the shared sanitized reporter.
- Green: focused 18/18 host-capable; safety, structure, hygiene, secrets, diff integrity, and exact host-capable
  broad Node discovery.
- Restricted child-spawn `EPERM` results are environment limitations and now retain their leaf cause.

No commit was authorized or created. Next action requires explicit commit authorization, followed by exact
committed-archive evidence and a matching authenticated CI artifact. Do not deploy `0383d47b` or start M1-M8,
host/provider/PIT/paper/runtime/public/live work.

## Update - 2026-07-29 commit and exact archive

- Atomic source commit: `8275a9acfc60dad36a15a24f5e8cde512307b6f8`.
- Focused gitlink/source-evidence contracts: 10/10.
- Committed-archive evidence: PASS, id `4346d24e-a72e-4ab5-b75b-1fb9be8a6ebe`, tree `b17485e...`,
  1,268 entries, all five lockfile roots, declared builds/tests, native 30/30, and secrets 905/0.
- The evidence artifact is ignored local state at
  `storage/logs/source_evidence/committed_archive-latest.json`.

No push or deployment occurred. The requested deployment still needs a named target/profile and matching
authenticated CI; do not infer a public bind, writer, provider, paper, bot, order, or live action.

## Update - 2026-07-29 private central host deployment

- Target: `vgbn-servers`, `192.168.4.135`, profile `central-host`.
- Published the reviewed lineage to `origin/main`; host GitHub fetch remains blocked by missing deploy-key
  authentication, so exact complete Git bundles were used for this manual deployment.
- Preserved the host's older dirty `e78e1788` worktree as
  `stash@{0}: pre-deploy server snapshot 2026-07-29 e78e1788`.
- Added a distinct host-local client token and generated seven owner-only service environment projections.
  The optional research scope points at `/app/storage/polymarket/scope.json`; that profile is stopped and will
  fail closed unless a valid file is supplied later.
- Host build exposed two real Docker runtime gaps. `15ef2840` removed nonexistent `native/` and ignored
  workspace copy inputs; `89771802` included the one tracked root backfill compatibility module required by
  the CLI. Focused deployment contracts pass 2/2 and direct data-command load passes.
- Canonical updater completed at `897718024b0d93fe44ee5920ef9157756499ca75`.
- Post-deploy proof: exact HEAD equals the deployed marker; checkout clean; only `web` and `backfill` running;
  web healthy at loopback `/health`; both restart counts zero and users `node`; runtime is `cloud-compute`,
  `LIVE_TRADING=false`, and `SOVEREIGN_EXECUTION_AUTHORIZED=false`.

No bot, paper, monitoring, research, public bind, order, or live execution started. Remaining operational
gates are a read-only GitHub deploy key, authenticated CI observation, optional updater timer installation,
freshness/DCS qualification, restart/rollback/recovery proof, and soak.

## Update - 2026-07-29 additional non-live central-host services

- Deployed `host-health` and `host-backup` under the explicit `monitoring` profile on `vgbn-servers`.
- First host-health result passed: canonical data fresh, disk healthy with about 71.3% free, and the backfill
  runner healthy.
- Host backup created `storage/backups/host/2026-07-29T14-45-37-330Z` with 2,155 files totaling
  4,608,604,600 bytes; the scheduled container remains running with zero restarts.
- `portfolio-monitor` failed closed on a critical `max_position_notional` breach for BTC
  (32,053.9 actual vs 25,000 limit) and unavailable Alpaca authentication. It was stopped after seven
  restarts; no threshold, position, or authentication state was altered.
- `polymarket-research` remains stopped because `storage/polymarket/scope.json` is absent.
- The paper bot, public exposure, order submission, and live execution remain stopped. The web endpoint
  remains healthy and loopback-only; the sole backfill writer was not recreated.

## Update - 2026-07-29 activation request preflight

- The user explicitly requested portfolio-monitor, research, paper, and live activation.
- No additional service started: portfolio monitoring is blocked by the existing exposure/authentication
  failure; research lacks its required scope file; provider-backed paper/research activation requires
  explicitly authorized restricted delegation; and central-host Compose has no live service and forces
  live/execution authorization false.
- Existing `web`, `backfill`, `host-health`, and `host-backup` runtime state was left unchanged.

## Update - 2026-07-29 activation-path triage and Bash recovery

- Portfolio monitor root cause: production CLI persistent mode already continues after a breach, but Compose
  wraps `--once` in a shell loop with `|| exit`, converting every valid critical assessment into a container
  restart. The bounded repair is to use the CLI-owned persistent loop and retain degraded/critical status.
- Polymarket research scope requires an explicit `token_ids` allowlist and matching active
  `markets[].tokens[]` entries. A real bounded scope still needs selected market/token IDs; none were invented.
- Server `storage/data/user_settings.json` already has `bot_autopilot:true` and `polymarket:true`; provider
  polling remains behind the repository's restricted-delegation requirement.
- The server Bash toolkit is an independent `master` repository with no `origin`. The missing untracked
  workstation installer was copied narrowly, hash-verified, and used to create six `~/.local/bin` links.
  Server Git history/remotes and the workstation's large dirty Bash worktree were preserved.
- Session ended with no finance source implementation, provider poll, research capture, paper cycle, order,
  live action, Git commit, or public exposure.

## Update - 2026-07-29 deep local current-run monitoring

- Observed the local `steamlinux` Docker runtime; the remote `vgbn-servers` host was not queried.
- Local containers use a July-27 mixed-lineage image with no revision label, not exact current source
  `89428649`. Monitor source matches `916c2964`; embedded Compose matches `e78e1788/0383d47b`.
- Portfolio monitor restart count advanced 170 to 179 while valid BTC breach status continued. The old image
  retained stale raw ETIMEDOUT state beside fresh metrics.
- Local paper bot is running healthy and advanced iteration 158 to 162. Direct container inspection proved
  cloud-compute mode with live and execution authorization false.
- Current source/data checks pass: exact committed-archive evidence for `89428649`, hygiene, structure 16/16,
  and local required-daily DCS 1.0. These do not qualify the stale local image or remote host.
- No container, provider configuration, threshold, paper state, order, public bind, or live state was changed.

## Update - runtime-integrity source implementation

- Implemented canonical persistent ownership for portfolio monitor, host health, and host backup while
  preserving direct one-shot contracts.
- Implemented backup due-time preservation, exact commit/tree image labels, immutable deployment image refs,
  manifest-derived active-set reconciliation, paper ledger/projection parity, research refusal, atomic
  deployment evidence, and per-service rollback.
- Verification is green: focused operational/architecture set, updater harness 11/11, environment, hygiene,
  structure 16/16, deployment, secrets 906/0, host-capable aggregate Node, and schema-v2 worktree evidence
  `81b7974c-9be6-42ad-97ab-e57bb60e4236`, captured before these append-only closeout records.
- Direct Compose render is blocked by missing required `.env.services/web.env`; no file was invented.
- Local runtime remains unchanged on `personal_finance:latest`; final read-only `docker ps` still showed the
  portfolio monitor restarting while bot/web/backfill/health/backup remained up.
- The exact-image rehearsal is deferred pending explicit commit authorization and a clean committed archive.
  Remote host, provider research, paper qualification, recovery drill, soak, public exposure, orders, and live
  execution remain outside this boundary.
