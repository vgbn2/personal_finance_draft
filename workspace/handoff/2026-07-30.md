# Session Handoff - 2026-07-30

## Update - vgbn exact-image deployment

- Published and deployed commit `9fef3ef79682d71ba21e9eaea66bfc1fef2d0a44` to `vgbn-servers`
  (`192.168.4.135`) through a verified complete Git bundle because host GitHub authentication remains absent.
- The first deployment attempt exposed an updater self-update boundary: the old shell process continued after
  fast-forwarding its own source. A repeat with the current script then exposed rollback tags being captured
  after a same-tag rebuild invalidated the running manifest-list ID.
- Fixed the rollback ordering in `9fef3ef7`: every active image is tagged before the deployment tag is rebuilt.
  The focused updater regression passed, the exact committed-archive verifier passed, and `origin/main` was
  updated.
- Final host HEAD equals the deployed marker. The evidence manifest binds `web`, `backfill`,
  `portfolio-monitor`, `host-health`, and `host-backup` to exact image
  `personal_finance:9fef3ef79682d71ba21e9eaea66bfc1fef2d0a44`, image ID `sha256:3106da1...`,
  source tree `de345412...`, and build contract 1.
- A confirmed-state updater rerun no-opped without rebuilding. Three stability samples plus a later monitor
  sample showed all five services running with zero restarts; web remained healthy and monitor cycles advanced
  from 1093 to 1094.
- The monitor truthfully remains in breach: BTC notional is about 31,984 versus the 25,000 limit, and Alpaca
  authentication is unavailable. The repaired persistent loop publishes that state instead of restarting.
- Every running service was inspected with `SOVEREIGN_RUNTIME_MODE=cloud-compute`, `LIVE_TRADING=false`, and
  `SOVEREIGN_EXECUTION_AUTHORIZED=false`. Paper bot and Polymarket research are absent; no order, public bind,
  or live execution was enabled.
- Temporary bundle remotes and bundle files were removed. The remote checkout is clean, but its `origin/main`
  tracking ref remains stale because the host still lacks a deploy key.

Next: install a read-only host deploy key, observe authenticated CI for `9fef3ef7`, then run explicit
restart/rollback/recovery and longer soak qualification. Do not weaken the BTC threshold to clear the breach.

## Update - deferred Alpaca monitor edge case

- The user confirmed Alpaca APIs should be configured and asked to leave the missing edge case for the next
  session.
- Read-only host inspection proved `.env.central` contains non-empty Alpaca key/secret values and the paper API
  URL. The backfill projection contains matching values, but `.env.services/portfolio-monitor.env` and the
  running monitor container contain no Alpaca key, secret, alias, or base URL.
- This matches the current environment manifest: `compose_portfolio_monitor` has no optional Alpaca keys and
  forbids the private environment class, while Alpaca credentials are allowed for `compose_backfill`.
- A second contract mismatch exists: gateway `aggregate_portfolio` constructs `Alpaca (Live)` with
  `paper:false`; the configured base URL is Alpaca Paper, and the portfolio monitor evaluates the `live` bucket
  rather than `live_paper`.
- Therefore `authentication_failed` does not prove invalid credentials. It currently reflects missing
  account-read projection plus live-versus-paper scope mismatch.

No source, environment, credential, container, provider, threshold, order, or runtime state changed. Next
session should route this through `codex` as one bounded account-read environment/scope contract fix.

## Update - Alpaca monitor source implementation

- Triage confirmed the monitor projection omitted Alpaca account keys/base URL and risk assessment selected
  only `live`; the gateway's distinct Live/Paper labels were already correct.
- Implemented one shared Alpaca scope contract. The central monitor defaults to Alpaca `paper` acquisition and
  combined `live + live_paper` risk assessment, so broker-paper visibility does not hide the live BTC boundary.
- Monitor projections now contain only the Alpaca key pair/base URL and scope knobs. Cloud-compute,
  live/execution false, absent trade PIN, and absent private-wallet credentials remain enforced.
- Invalid scopes fail closed; missing paper credentials remain explicit; scope changes reset incompatible
  peak-equity state; legacy flat snapshots remain supported.
- Fresh local integrity is 92/92 with 0 missing/stale/unexplained and DCS 1.0. Focused tests pass 39/39 with one
  sandbox-only nested-child skip; host-capable aggregate Node, environment, TypeScript, hygiene, structure
  16/16, and diff integrity pass.

The user authorized committing all remaining changes and synchronizing the exact revision to `vgbn-servers`.
Committed-source and deployed-runtime proof remain pending at this boundary. No credential value, provider,
container, threshold, paper, order, public, or live state changed during source implementation.

## Update - Alpaca monitor commit and server cutover

- Committed all reviewed remaining changes as `b5f35e8b8c7a7e5a8ff68f4c68aabe05287e32a9` and published
  `origin/main`. Exact committed-archive evidence passed with id
  `f0fdadf7-1a5b-43e4-8c37-2ed09b26ab9f`, tree `a5ceced30ca535151d6a3fc0b1ba8839d9a6f03e`,
  1,273 source files, and a clean archive.
- Synchronized the clean server checkout through an on-host-verified complete Git bundle. The host still
  lacks GitHub authentication, so its `origin/main` tracking ref remains stale.
- Built exact image `personal_finance:b5f35e8b8c7a7e5a8ff68f4c68aabe05287e32a9`, image ID
  `sha256:3d306fb405b54e2365617a7fbe536b7044361552fc2b7e03372fb36c01ce78fa`, with revision, source-tree,
  and build-contract labels.
- Atomically regenerated only `.env.services/portfolio-monitor.env` from the existing owner-only central
  environment. The prior monitor projection was retained in `/tmp`; central tokens and other service
  projections were not rotated.
- Recreated only `portfolio-monitor`. `web`, `backfill`, `host-health`, and `host-backup` retained their
  pre-cutover container IDs and exact `9fef3ef7` image. The monitor runs the exact `b5f35e8b` image with zero
  restarts, combined risk scope, paper Alpaca scope, live/execution false, and no trade PIN or private wallet.
- The structured status moved from the old `live`/`Alpaca (Live)` result at cycle 1122 to
  `both`/`Alpaca (Paper)` after cutover. The BTC critical breach remains real and unchanged at a 25,000 limit.
- Alpaca still returns `authentication_failed`. Fingerprint-only proof shows monitor, central, and backfill
  key/secret values match and the base is the paper endpoint, so this is now a provider credential-validity
  gate rather than the repaired projection/scope defect. No value was exposed and the failure was not hidden.

This was intentionally a service-scoped deployment. The global deployed-head marker and five-service
deployment manifest remain at the prior coherent `9fef3ef7` deployment; do not claim all five containers run
`b5f35e8b`. Paper bot, research, orders, public exposure, threshold changes, and live execution stayed out of
scope.

## Update - differentiated Alpaca credentials and final server cutover

- Replaced ambiguous Alpaca configuration with explicit `ALPACA_PAPER_*` and `ALPACA_LIVE_*` ownership.
  Generic names remain a compatibility fallback only when their base URL matches the requested account scope.
- Classified Live credentials as execution-class and Paper credentials as account-read provider secrets.
  The central host and monitor receive Paper names only; runtime remains cloud-compute with live and execution
  authorization false.
- Focused resolver, backfill, setup/doctor, environment, Compose, TypeScript, hygiene, structure, secrets, and
  full host-capable tests passed. Exact committed-archive evidence
  `22408daa-dd36-492b-aec8-73c2224e97e6` passed for published commit
  `65df1d1d9e3bfd6a30fcebab0fad5eda420523ac`, tree
  `0cd1ab64cb2e927e97d1a985dabe504d713835b2`.
- Synchronized the exact commit to `vgbn-servers` through a verified complete Git bundle and built image
  `personal_finance:65df1d1d9e3bfd6a30fcebab0fad5eda420523ac`,
  `sha256:3667f6ce732e26aaad9492d70a9b0ea16ddd8d62813e59564dd1c99609bd3e2b`.
  Recreated only `portfolio-monitor`; the other four active container IDs stayed unchanged.
- The server central environment and monitor projection are owner-only and contain only the scoped Paper
  credential set and Paper endpoint. They contain no Live or legacy Alpaca credential names.
- Structured cycles 1166 through 1171 published normally with no monitor restart. Gate.io connects; Alpaca is
  correctly labeled `Alpaca (Paper)` but still returns `authentication_failed`. The BTC critical breach
  remains against the unchanged 25,000 limit.
- Added an API Authentication Gate to the canonical `blast-through` skill and synchronized its discovery
  mirror. It requires separate evidence for presence, projection, endpoint, runtime consumption, and provider
  acceptance, and forbids secret/raw-response disclosure.
- Securely removed the local credential transfer file, local and remote bundle files, and the remote prior
  monitor projection. Those temporary files are not recoverable.

Next: validate or regenerate the Paper key in the Alpaca Paper account and use `blast-through` to inspect
structured status again. Do not revisit source projection/scope unless new evidence contradicts this
deployment. The server remains intentionally mixed revision: monitor `65df1d1d`; other four services and
global marker `9fef3ef7`.

## Update - Alpaca and monitor read-only follow-up

- A fresh read-only SSH check found `portfolio-monitor` still running exact image `65df1d1d` with zero
  restarts. The sampled 30-minute log advanced normally through cycle 1463 at the configured one-minute
  cadence.
- Cycle 1463 remains a truthful breach: BTC notional is 32,015.85 against the unchanged 25,000 limit,
  Gate.io is connected, and `Alpaca (Paper)` remains `authentication_failed`.
- The prior configuration gates remain closed: scoped Paper names, Paper endpoint, least-privilege monitor
  projection, combined risk scope, runtime consumption, and non-live safety are already proved. Provider
  acceptance is the open gate; validate or regenerate the key pair in the Alpaca Paper account rather than
  revisiting source projection or renaming variables.
- A separate P2 observability defect is confirmed. The portfolio heartbeat refreshed at cycle 1463 and its
  attempt count reached 396, but `last_attempt_at` remained at `2026-07-29T14:45:37.857Z`. The shared writer
  preserves the prior timestamp unless callers supply one, while the portfolio monitor passes only the
  attempted boolean. This does not stop polling, but it can mislead heartbeat consumers.
- Host inventory for the homelab recommendation: Ubuntu 26.04, Linux 7.0, 15.0 GiB RAM, Docker 29.1.3,
  Compose 2.40.3, no Cockpit socket, and no detected Portainer/Dockge/Homarr/CasaOS/Yacht/Beszel/Netdata
  container. Cockpit is the preferred first management UI; container GUIs must not bypass the canonical
  updater or exact-image evidence.

No provider request, credential change, container restart, threshold change, paper cycle, order, public
exposure, or live execution occurred.

## Update - blast-through fault-domain and stub-causality contract

- The canonical `blast-through` skill now requires attribution for every confirmed finding and every reviewed
  grade below A.
- Required fields are the first proved failing boundary, fault domain, repair owner, causal mechanism, stub
  involvement, confidence, alternatives checked, and the first discriminating check.
- Fault domains distinguish project source, owned host/deployment, operator credentials/configuration,
  external provider, sandbox/environment, mixed causes, and unresolved evidence.
- Stub classifications distinguish production stubs, test-only stubs, silent fallbacks, compatibility shims,
  real adapters, no stub, and unresolved evidence. A real adapter or test double cannot be mislabeled as the
  production cause.
- Authentication attribution cannot assign a normalized 401 to the provider. It must first close presence,
  projection, endpoint, runtime-consumption, and provider-acceptance layers.
- The canonical package and `.agents` discovery mirror are synchronized. Skill validation, mirror check,
  focused repository-skill contract, and `git diff --check` pass.

Applied to the current defects: Alpaca uses the real official-SDK adapter with simulation disabled, so no
production stub causes the failure. The first failed layer is provider acceptance of the operator-controlled
Paper credential/account state; the exact cause remains a credential/account candidate until a regenerated
Paper key is checked. The stale heartbeat attempt timestamp is an `our_source` defect in the shared heartbeat
writer/caller contract, with no stub involved.

## Update - Alpaca authentication and heartbeat fix plan

- The user clarified that the rejected Alpaca Paper keys were generated recently. Repeated rotation is no
  longer the first action.
- Added `workspace/plans/ALPACA_PAPER_AUTH_AND_HEARTBEAT_FIX_PLAN.md` with a GO WITH DIAGNOSTIC GATES decision.
- ALP-A0 freezes exact monitor/runtime evidence and checks redacted credential shape, projection, override,
  endpoint, time, and transport boundaries.
- ALP-A1 implements and locally tests a redacted Paper-only diagnostic, then—only with explicit authorization—
  compares raw HTTPS `/v2/account` acceptance against the existing SDK `getAccount()` call using the same
  owner-only monitor environment.
- ALP-A2 routes each raw/SDK outcome to the correct repair owner. An SDK upgrade remains NO-GO without the
  existing exact v3-to-v4 compatibility map.
- HB-A1 independently fixes the proved heartbeat `last_attempt_at` semantics with multi-cycle regressions.
- Any deployment is monitor-only, exact-image, rollback-backed, and must preserve combined risk scope, the BTC
  25,000 threshold, and live/execution false.

No provider probe, source implementation, credential change, container action, order, public exposure, or
live action ran.

## Position sizing research

- Added `workspace/research/POSITION_SIZING_CONTRACT_DOLLAR_LOT_RESEARCH.md`.
- Overall cross-asset sizing grade is D+: whole-unit/dollar and prediction-share paths exist, but there is no
  authoritative intent-to-instrument-to-broker normalization contract.
- P1: strategy automation substitutes `$1` for a missing/invalid signal price before calculating quantity.
- P1: contract multipliers and lot semantics are absent; the current MT5 production-source bridge is a
  metadata/routing scaffold and the MQL5 artifact exports data rather than submitting orders.
- P2: dollar sizing floors every asset to whole units even though Alpaca transport accepts fractions.
- P2: `amount:USD` loses its intent, quote, timestamp, rounding, and residual evidence when converted to a bare
  quantity.
- P2: the guided Polymarket order path enforces the orderbook minimum, but the direct shared core validates
  only positive quantity.
- Ranked next implementation: fail closed on invalid prices, introduce one typed sizing intent/decision,
  qualify provider instrument metadata, add broker-specific contract tests, then update documentation and
  perform separate provider-paper qualification.
- The supplied Alpaca and Polymarket CLIs are optional diagnostic/reference candidates, not required runtime
  dependencies. Alpaca CLI is absent locally; `backend/polymarket-cli` is an unmaterialized gitlink. External
  inspection requires user-authorized restricted delegation under the repository gate.

No provider request, installation, order, credential, runtime, or production source change occurred.

## Canonical paper sizing mass implementation

- Lifecycle: proposed -> preflight -> GO WITH FIXES -> implemented -> verified -> reviewed -> closed.
- Added `shared/lib/trading/position_sizing.js` as the one sizing owner for units, notional, risk budget,
  contracts, and lots.
- Closed the unsafe strategy `$1` fallback. Missing, zero, negative, null, and non-finite reference prices now
  fail closed before quantity dispatch.
- The internal Polymarket paper engine now supports `notional`, `units`, and `risk_budget` intent; contracts and
  lots are rejected because they are not prediction-share units.
- Normalization enforces quantity step, orderbook minimum size when available, virtual cash, maximum-position
  USD, non-negative risk stop, and BUY stop below reference price.
- The append-only ledger stores requested intent, reference price, raw and rounded quantities, metadata source,
  rounding rule, binding caps, projected notional, and residual.
- `polymarket paper-run` and `run bot paper` accept `--sizing-mode`, `--size`, `--stop-price`, and
  `--max-position-usd`; persistent runner arguments contain no `--live`.
- Verification: focused 29/29; gateway TypeScript pass; host-capable full `npm test` pass; environment manifest,
  hygiene, structure 17/17, and diff integrity pass.
- Sandbox aggregate failures were only nested-process `EPERM`; the exact host-capable rerun passed.
- Large-file decision: `backend/gateway/src/index.ts` and `strategy.js` received narrow seam edits only and were
  not split; `polymarket_paper.js` remains one cohesive paper lifecycle while sizing math moved out.
- Grade movement: internal Polymarket paper sizing D -> A- source/test evidence; overall cross-asset sizing D+
  -> C. Provider-qualified metadata, live/broker-paper normalization, MT5 order execution, deployment, recovery,
  and soak remain open.
- The user-supplied external CLI sources were not fetched or installed. Restricted upstream review still
  requires explicit delegation authorization under the repository gate.

No provider request, credential use, external install, live/broker-paper order, container, deployment, or
persistent production paper cycle ran.

## Alpaca Paper execution closeout

- Commits `47b99d9e` and `ea6d76bd` deployed to `vgbn-servers` via complete Git bundle and canonical updater.
- `--paper-provider` is a bounded one-shot route, not a scheduler: it forces Alpaca Paper, rejects `--live`,
  defaults to `$25` max notional, persists strategy labels, and retains native risk checks.
- Qualification order accepted by Alpaca Paper: AAPL buy `0.01` market, `$0.25`, strategy `scalp_probe`, order
  ID `707e8077-971e-4b20-85b3-0911e2726d31`.
- Host verification: `LIVE_TRADING=false`, `SOVEREIGN_EXECUTION_AUTHORIZED=false`, runtime `cloud-compute`;
  monitor reports `Alpaca (Paper)` connected. Existing BTC max-position breach remains truthful.
- Verification: gateway TypeScript and focused gateway tests pass; broader sovereign CLI suite remains
  inconclusive because its subprocess exits during suite startup. No Alpaca recurring strategy loop was started.
- First next action: only if requested, design and review an explicit Alpaca Paper schedule and multi-strategy
  qualification plan; do not infer a cadence from this one-shot probe.

## Update - 2026-07-30 recurring Alpaca Paper strategy closeout

- Commit `c868fc3b` added `run bot alpaca-paper` and routed the existing strategy automation engine through the
  explicit Alpaca Paper provider path; live execution remains separate and disabled.
- The host now has a detached 15-minute Alpaca Paper loop scanning 13 enabled registered strategies with a `$25`
  per-order cap. The first one-pass validation completed without errors or order signals.
- Existing Polymarket Paper remains a separate one-minute loop. Host safety remains `LIVE_TRADING=false` and
  `SOVEREIGN_EXECUTION_AUTHORIZED=false`.
- Qualification boundary: the Alpaca loop is not independently Compose-managed and is not restart/recovery or
  soak-qualified; container recreation will stop it. First next action is an independently managed service with
  duplicate-loop prevention and restart/stop/rollback proof.
- Session ended at the user's explicit boundary.

## Update - 2026-07-30 skill sync and repository professionalism audit

- **Skill Sync**: Executed `node scripts/dev/sync_repo_skills.js --write`, bringing all 10 canonical packages in `skills/` into 1:1 parity with `.agents/skills/` mirrors.
- **Git Submodule Cleanup**: Identified and staged removal of `backend/polymarket-cli` (an invalid gitlink `160000` entry that had no `.gitmodules` mapping and was breaking `git submodule status`). `git submodule status` now completes cleanly with exit code `0`.
- **Repository Audit & Test Verification**:
  - `npm run hygiene`: Passed 100% across all 6 categories (Git noise, symlinks, agent skills, code markers, repository integrity, docs alignment).
  - `npm run test:structure`: Passed 100% (17 subtests).
  - `npm test`: Passed 100% across all architecture, CLI, API, TUI, settings, and risk test suites.
  - Verified [README.md](file:///home/vgbn1/Documents/codeptit/personal_finance_draft/README.md) layout, quick start commands, and multi-root dependency instructions.
- Session closed via `session-orchestrator`.

