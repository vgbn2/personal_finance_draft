# Handoff — 2026-08-28

## Update - 2026-08-28 — Fractional Unit Sizing, Alpaca Tradable Asset Filter & State Phase Anchor Fix

### 1. Context & Objectives
- Addressed live bot sizing rejections reported in `flaw_monitor.log` on `hpdesk` (`vgbn-server@100.122.7.7`):
  `Sizing rejected for BNBUSDT / BTCUSDT / SPY / QQQ: below_quantity_step`
- Resolved integer quantity truncations on sub-$100 notional allocations for high-priced equities (SPY, QQQ) and crypto (BTC, ETH, SOL).
- Fixed `workspace/STATE.md` current phase anchor title format to align with sovereign CLI test contracts (`/\bACTIVE\b/i`).
- Enhanced `skills/blast-through/SKILL.md` and `skills/blast-through/references/audit-modes.md` to require active runtime log inspection (`flaw_monitor.log`, `storage/data/logs/*.jsonl`, remote container logs) as part of all audit workflows.

### 2. Changes Implemented
1. **`shared/lib/brokers/alpaca_env.js`**:
   - Added `ALPACA_SUPPORTED_CRYPTO_PAIRS` and `isAlpacaTradable(symbol)` to filter out unsupported instruments (e.g. `BNBUSDT`, `APTUSDT`) before order dispatch.
2. **`backend/cli/commands/strategy/strategy_presenter.js`**:
   - Implemented `resolveInstrumentQuantityStep(symbol, allowFractional)`: returns `0.001` for US equities and `0.0001` for crypto when fractional sizing is enabled.
   - Enhanced `buildStrategySizingDecision` to accept `allowFractional` and `quantityStep`, preserving legacy whole-unit contract as default.
3. **`backend/cli/commands/strategy/strategy.js` (`runAutomationPass`)**:
   - Enabled fractional sizing for `providerPaper` (`alpaca-paper`) mode.
   - Replaced integer flooring (`Math.floor`) with `roundDownToStep(approvedNotional / currentPrice, step)`.
   - Added `isAlpacaTradable` pre-filter before strategy signal order generation.
4. **`workspace/STATE.md`**:
   - Aligned Current Phase header to `Unified Ingestion, Local Rollup, Fast-Path Live Signal Derivation & HPDesk Soak - ACTIVE`.
5. **`tests/scripts/safety/strategy_sizing.test.js`**:
   - Added tests verifying fractional unit contract sizing on equities (`SPY` @ $500 -> 0.1 qty) and crypto (`BTCUSDT` @ $65,000 -> 0.001 qty) while verifying legacy whole-unit safety.

### 3. Next Session Focus
- Synchronize and deploy the worktree changes to `hpdesk` (`vgbn-server@100.122.7.7`).
- Verify `flaw_monitor.log` confirming zero `below_quantity_step` errors on active sub-$100 bot cycles.
