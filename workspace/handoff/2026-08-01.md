# Handoff - 2026-08-01

## Update - 2026-08-01 session 117 — Compose-managed Alpaca Paper loop

- Converted `run bot alpaca-paper` from its internal detached timer into a canonical named `startLoop` owner (`alpaca_paper_bot`) with `installShutdownHandlers()`, duplicate-name rejection, status/heartbeat publication, and one-shot strategy passes.
- Added the isolated Compose service `bot-alpaca-paper` under profile `paper-alpaca`, with a 15-minute default cadence, trust score 70, Paper notional cap 25, `restart: unless-stopped`, and fixed non-live overrides.
- Added an exact environment-manifest projection requiring only Alpaca Paper credentials and forbidding central/execution environment classes.
- Feature commit `aa84ef56` was merged into local `main` at `9d0a8e30`; feature-exerciser evidence was committed at `3a877a52`. Nothing was pushed. The separate `worktree-alpaca-paper-compose` worktree/branch remains.
- Verification: focused merged-source bundle 43/43; feature-exerciser source/loop/Compose/environment bundle 28/28; canonical loop tests 6/6; safety gate passed; Compose render, manifest checker, syntax, secret scan (911/0), hygiene, and diff checks passed. The broad `npm test` had one unrelated structure-contract failure caused by passing `node_modules` without a trailing slash to a directory-only ignore rule; direct `git check-ignore --no-index node_modules/` passes.
- Runtime was not activated: no Alpaca Paper process/container is running on `steamlinux`, `bot_autopilot` remains disabled, no provider poll or Paper/live order ran, and no runtime setting or service state changed. A direct real CLI one-shot probe was denied by the session classifier.
- First next action: if the user explicitly authorizes Paper runtime activation, generate/review `.env.services/bot-alpaca-paper.env`, enable `bot_autopilot`, start only `bot-alpaca-paper`, and separately verify first-cycle logs, graceful stop, restart, rollback, and no duplicate detached process. Otherwise leave it inactive.
