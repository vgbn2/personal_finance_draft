# Handoff — 2026-06-13

## Update - 2026-06-13 session 24 (Claude orchestrator) — sessions 23/23b batch verified + committed; 5m crypto+equity CLOSED

**Context:** Boot per session-orchestrator. The harness-tracked background task for the session-22
18-symbol 1825d crypto rerun reported completed (exit 0); sessions 23/23b (run outside Claude) had
implemented the synthetic-5m guard and US-equity 5m Phase 2 but left the ENTIRE batch uncommitted
(19 modified files + 1 new test — 4th occurrence of the "docs say done, git says uncommitted"
drift class). User chose: verify + commit, and verify the Codex equity backfill claims.

**Independent verification (all claims held):**
- Backfill task JSON: all 18 symbols accounted for, 0 errors (13 full-depth; PEPE 326,571 /
  WIF 238,780 / POL 183,534 listing-bounded; RNDRUSDT 969 stale/delisted).
- `readTsIndex` probe over `storage/data/ts/*_5m.bin`: **33 Alpaca-backed equity bins,
  3,101,322 merged `provider=alpaca` rows — exact match** to session 23b's claim (min ARM=51,993,
  max QQQ=112,021). Crypto: 13 long-listed symbols at ≥525,506 bars (5 slightly above — newer
  bars merged on top, so don't probe for "exactly 525,506").
- Full diff reviewed per codex-review workflow: no hallucinated conventions; alpaca.js pagination
  has a monotonic progress guard; per-page push-spreads are bounded (≤10k, under the V8 limit);
  equity-deep-backfill failure accounting matches the session-22 loud-failure semantics.
- Full suite re-run by orchestrator: **395/395 exit 0** (confirmed baseline).

**Commits (4):**
- `a19d6323` feat(data): synthetic 5m experimental-only at the consumer boundary
  (validateSnapshot `synthetic_lower_timeframe`, ml dump default exclusion +
  `--include-experimental-5m`).
- `60458a7a` feat(data): native US-equity 5m Phase 2 — Alpaca paginated
  (`equity-deep-backfill`, 5m→5Min mapping, next_page_token, feed=iex/adjustment=split,
  10k equity chunks, no daily-synthesis fallthrough, provider pinning outside section chains).
- `58130cb9` docs(workspace): sessions 23/23b/24 close-out + runtime artifacts.
- (this docs commit)

**Design note (deliberate):** `ml dump` excludes ALL non-crypto 5m by default — even native
Alpaca equity 5m — until the equity session-gap guard and ML 5m cap/perf gates land.
`isExperimentalSynthetic5mRecord` in `shared/lib/ml/dataset.js` is the single classifier.

**Untracked user artifacts at repo root (NOT committed, user to clean up):**
`personal_finance_draft-state.zip` (136MB), `personal_finance_draft.bundle` (400MB), and
`vgbn1@vgbn-` (400MB, byte-size-identical to the bundle — looks like an scp invocation whose
remote destination became a local filename). Created 2026-06-12 17:53.

**Carryovers (unchanged):** 5m Phase 3 indices/commodities/FX provider/budget decision (or Yahoo
60-day accumulate-forward stop-gap); equity 5m session-gap guard before indicator/backtest
consumption; ML 5m caps/perf gates; NDJSON streaming user sign-off; CLI lazy-requires (optional);
merge `feat/ml-onnx-section` → main = user decision.
