# Handoff — 2026-07-05

## Update - 2026-07-05 session 64 — Connective-tissue mass-implement

**What changed:**
- `shared/lib/providers/alpaca.js` now uses `resolveAlpacaSettings()`, so documented
  `ALPACA_SECRET_KEY` works for Alpaca data fetches as well as gateway/setup flows.
- `backend/scripts/data_ops/ingest_market_data/manifests.js` now throws structured
  `not_implemented` errors for unfinished provider lanes instead of returning `{}`:
  `pmi`, `flight`, `crypto_tx`, `holdings`, `onchain`, `breadth`, and `fxapi`.
- `backend/gateway/package.json` / lockfile now declare direct `axios`.
- Added `tests/scripts/data/ingest/ingest_manifest_contract.test.js`.
- Added Alpaca alias regression coverage in `tests/scripts/data/backfill/equity_5m_backfill.test.js`.

**Verified:**
- `node --test tests/scripts/data/backfill/equity_5m_backfill.test.js`
- `node --test tests/scripts/data/ingest/ingest_manifest_contract.test.js`
- direct `ALPACA_SECRET_KEY` provider probe returned one stubbed record
- direct PMI manifest probe returned `not_implemented`
- `npm ls --prefix backend/gateway --depth=0`
- `npx tsc -p backend/gateway/tsconfig.json --noEmit`
- `npm run hygiene`

**Remaining:**
- Placeholder provider lanes still need real implementation if those data families should produce
  live records. Current behavior is now explicit and honest, not fake data.
- Low-priority API scaffold cleanup remains open.

## Update - 2026-07-05 session 64 — Connective-tissue blast-through

**What was checked:**
- Upgraded `blast-through` connective-tissue mode: stubs, strings, env/docs parity, dependency
  declarations, import/orphan candidates, TUI command strings, and API scaffold.
- `graphify-out/` is absent, so this used live repo scans plus direct file reads.

**Findings recorded in `workspace/DEV_REVIEW.md`:**
- Enabled ingest families still route to no-op provider stubs: `pmi`, `flight`, `crypto_tx`,
  `holdings`, `onchain`, and `breadth`.
- Alpaca market-data provider only reads `ALPACA_API_SECRET`; `.env.example` and setup tooling use
  `ALPACA_SECRET_KEY`.
- Gateway directly imports `axios` in production paths but does not declare it in
  `backend/gateway/package.json`.
- API has stale Express-style middleware/helper scaffold beside the active custom HTTP server.

**Verified-good:**
- Settings & Preferences is wired now.
- TUI command ids with spaces are intentional and split into argv by `dashboard_exec.buildArgv()`.
- All package-root `npm ls` checks passed.
- `npm run hygiene` passed.

## Update - 2026-07-05 session 64 — Fix Polymarket derive-creds gateway startup

**Boot context:** session 64 started from `workspace/PROMPT_LOG.md` and the 2026-07-04 handoff.

**What changed:**
- Replaced eager `import 'dotenv/config'` in `backend/gateway/src/index.ts`,
  `backend/gateway/src/cycle.ts`, and `backend/gateway/src/bot_state.ts` with the repo-native
  loader `shared/lib/runtime/env.js`. This removes the hard dependency on an installed
  `dotenv` package for gateway startup.
- Deferred the `@alpacahq/alpaca-trade-api` require until `AlpacaAdapter` is actually
  constructed. That keeps Polymarket-only commands from failing on an unrelated missing Alpaca
  package during module load.

**Verified:**
- `node backend/cli/sovereign_cli.js polymarket derive-creds` now reaches the intended
  `POLYMARKET_PRIVATE_KEY not set in .env` check instead of crashing on missing modules.

**Remaining:**
- The command still needs a configured `POLYMARKET_PRIVATE_KEY` to emit credentials, which is the
  expected runtime requirement.

## Update - 2026-07-05 session 64 — Deep blast-through after Windows→Ubuntu migration

**What was checked:**
- `bash -n start_local.sh`
- `./sv status`
- `node backend/cli/sovereign_cli.js status`
- `timeout 10s ./start_local.sh`

**What was found:**
- The Linux bootstrap path has one concrete fragility: `start_local.sh` calls `npx tsx` even
  though the repo does not declare `tsx` in the root install path and has no local `tsx` binary.
  That means a fresh Ubuntu machine is coupled to external `npx` resolution/cache/global install
  instead of only the repo’s documented dependencies.

**Environment note:**
- The dashboard bind error seen in the sandbox (`listen EPERM` on `127.0.0.1:8787`) was treated as
  an environment restriction, not a repo defect.

## Update - 2026-07-05 session 64 — Dependency blast-through and mass-implement

**What changed:**
- `start_local.sh` now uses `node backend/cli/lib/run_trade_gateway.js --demo` instead of `npx tsx`.
- Root `package.json`/`package-lock.json` now include `@alpacahq/alpaca-trade-api` and `ethers`.
- Installed nested package roots for `backend/gateway`, `backend/mcp_server`, and `Frontend/dashboard`.
- `README.md` now documents the required dependency installs for root, API, gateway, MCP, frontend,
  and Ubuntu native C++ tooling.

**Verified:**
- `npm ls --depth=0`
- `npm ls --prefix backend/gateway --depth=0`
- `npm ls --prefix backend/mcp_server --depth=0`
- `npm ls --prefix Frontend/dashboard --depth=0`
- `npx tsc -p backend/gateway/tsconfig.json --noEmit`
- `npm --prefix backend/mcp_server run build`
- `node backend/cli/lib/run_trade_gateway.js --demo`
- `bash -n start_local.sh`
- `timeout 8s ./start_local.sh`

**Remaining:**
- The sandbox can still reject binding `127.0.0.1:8787` with `listen EPERM`; verify the web API in a
  normal terminal if that remains visible outside this environment.

## Update - 2026-07-05 session 66 — Windows env sync

**What changed:**
- Synced the active workspace `.env` from the Windows draft copy at
  `/mnt/windows/Users/Lenovo/Desktop/VGBN/.vscode/CODEPTIT/personal_finance_draft/.env`.
- Verified the Polymarket credential gate now sees the full set again:
  `POLYMARKET_PRIVATE_KEY`, `POLYMARKET_API_KEY`, `POLYMARKET_API_SECRET`,
  and `POLYMARKET_API_PASSPHRASE` are all present in the active env.
- `buildPolymarketReport(process.env)` now returns `ok: true`, and
  `commandPolymarket(['portfolio'])` now gets past the config check and reaches the
  live-network path (`getaddrinfo EAI_AGAIN` here, which is environment/network, not config).

**Remaining:**
- Polymarket portfolio access is currently blocked by network/DNS reachability to `clob.polymarket.com`.
