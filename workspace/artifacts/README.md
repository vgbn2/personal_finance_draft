# Workspace Artifacts

Bounded outputs produced during active reviews or implementation live here when they must be retained for the next session.

Artifacts are operational evidence, not canonical engineering documentation. Each artifact should identify its producer, source anchor, purpose, retention condition, and canonical docs link if knowledge was promoted. Generated or reproducible outputs should remain ignored unless repository policy requires tracking.