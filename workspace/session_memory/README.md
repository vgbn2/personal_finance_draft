# Session Memory Archive

Split and archived session-memory records live here to preserve continuity without loading the full history on every boot.

These files are dated operational evidence. Use the relevant entry to recover context, then verify current claims against source, tests, `workspace/STATE.md`, and canonical docs. Do not cite a memory entry as the current engineering contract.