/**
 * Exercise 4: Engine Communication
 * 
 * Objective: Set up a heartbeat or polling interval for IntelligenceEngine.
 * 
 * Instructions:
 * 1. In IntelligenceEngine's 'initialize()', add a setInterval.
 * 2. Every 5 seconds, iterate through all symbols and compute their scores.
 * 3. Log the results to the console.
 */
