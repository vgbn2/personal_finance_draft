# Direct Vite development only. Production uses the dashboard's same origin.
VITE_API_URL="http://localhost:8787"

# Public browser credentials only. Never put a Supabase secret/service-role key here.
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-publishable-or-anon-key"
