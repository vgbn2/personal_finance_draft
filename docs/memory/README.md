# Historical Documentation Mirrors

These files are historical mirrors retained for continuity. They are not canonical engineering documentation and may lag the owning workspace history.

Use current `workspace/` records to reconstruct dated events, then verify durable claims against source and tests before promotion into `docs/modules/`, `docs/atlas/`, or another current documentation owner.