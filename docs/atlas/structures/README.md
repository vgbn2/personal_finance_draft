# Structure Atlas

Structure records document types, schemas, persisted representations, and state machines that carry important system invariants.

Each record identifies the canonical mutation owner, field semantics and units, legal states, producers/consumers, persistence and compatibility, concurrency and recovery rules, cost model, and focused verification.

Use [TEMPLATE.md](TEMPLATE.md).