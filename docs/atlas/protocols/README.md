# Protocol Atlas

Protocol records document contracts across process, language, API, CLI, file, provider, and generated-artifact boundaries.

Each record defines participants and authority, message shapes and units, sequencing, success/error/degraded semantics, retry/timeout/idempotency behavior, trust and compatibility boundaries, observability, recovery, source owners, and contract tests.

Use [TEMPLATE.md](TEMPLATE.md).