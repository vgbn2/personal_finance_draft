---
phase: 7
plan: 2
wave: 1
---

# Plan 7.2: Polymarket CLOB Live API & Execution

## Objective
Graduate the Polymarket integration from a stubbed orderbook listener to a fully authenticated CLOB client capable of real-time position reconciliation and trade execution feedback.

## Context
- .gsd/SPEC.md
- app/core/reconciliation.py
- app/core/data_feed.py

## Tasks

<task type="auto">
  <name>Integrate Official Polymarket SDK</name>
  <files>
    - app/core/data_feed.py
    - requirements.txt
  </files>
  <action>
    - Add `py-polymarket-clob` to `requirements.txt`.
    - Refactor `PolymarketWS` to use the SDK for authenticated connections.
    - Implement `listen_for_user_updates` to capture trade fill confirmations.
  </action>
  <verify>
    - Python script verifying successful authentication with `API_KEY` (if provided by user in .env).
  </verify>
  <done>
    - WS connection to `user` channel successfully established.
  </done>
</task>

<task type="auto">
  <name>Live Position Reconciliation</name>
  <files>
    - app/core/reconciliation.py
  </files>
  <action>
    - Replace `_fetch_exchange_positions` STUB with actual call to `clob_client.get_open_positions()`.
    - Implement rate-limiting decorator to prevent 429s during loops.
  </action>
  <verify>
    - Run `python -m app.core.reconciliation` against a real (or staging) API.
  </verify>
  <done>
    - Reconciliation report shows "actual" positions from the exchange.
  </done>
</task>

## Success Criteria
- [ ] Authenticated connection to Polymarket CLOB.
- [ ] Real-world position data appearing in reconciliation reports.
