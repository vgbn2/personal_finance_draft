---
phase: 7
plan: 3
wave: 2
---

# Plan 7.3: Production Hardening & E2E Validation

## Objective
Finalize security, rate-limiting, and end-to-end reliability for production deployment. Ensure all secrets are externally managed and the system handles network instability gracefully.

## Context
- .gsd/SPEC.md
- app/utils/config.py
- .env.template

## Tasks

<task type="auto">
  <name>Secure Secret Management & Template</name>
  <files>
    - .env.template
    - app/utils/config.py
  </files>
  <action>
    - Create comprehensive `.env.template` with all required API keys (Binance, Deribit, Polymarket).
    - Ensure `config_manager` throws an error if required secrets are missing in production mode.
  </action>
  <verify>
    - Test boot with missing keys to confirm validation logic.
  </verify>
  <done>
    - Validation successfully blocks boot without required secrets.
  </done>
</task>

<task type="auto">
  <name>Rate Limit & Backoff Logic</name>
  <files>
    - app/utils/network.py (NEW)
    - app/core/data_feed.py
  </files>
  <action>
    - Implement a generic `BackoffHandler` and `RateLimiter` (Leaky Bucket).
    - Apply to all external exchange clients.
  </action>
  <verify>
    - Mock testing of 429 responses to trigger backoff.
  </verify>
  <done>
    - System gracefully waits and retries on rate limit hits.
  </done>
</task>

<task type="auto">
  <name>Full E2E Signal-to-Log Verification</name>
  <files>
    - scripts/e2e_live_test.py (NEW)
  </files>
  <action>
    - Create a script that boots the full system, receives one live packet, generates a mock signal, and verifies the audit log entry.
  </action>
  <verify>
    - Run `python scripts/e2e_live_test.py`.
  </verify>
  <done>
    - Audit log confirms end-to-end data flow.
  </done>
</task>

## Success Criteria
- [ ] Zero secrets in codebase.
- [ ] Resilience to 429 errors.
- [ ] Verified audit trail for live events.
