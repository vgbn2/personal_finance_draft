---
phase: 7
plan: 1
wave: 1
---

# Plan 7.1: Live Data Ingestion (WebSockets)

## Objective
Replace REST polling in `BinanceClient` and `DeribitClient` with real-time WebSocket streams for OHLCV and market data. Update `feed_aggregator.py` to handle push-based updates.

## Context
- .gsd/SPEC.md
- app/core/data_feed.py
- .gsd/phases/7/RESEARCH.md

## Tasks

<task type="auto">
  <name>Implement Binance & Deribit WebSocket Clients</name>
  <files>
    - app/core/data_feed.py
  </files>
  <action>
    - Implement `BinanceWSClient` and `DeribitWSClient` using `asyncio` and `websockets` (or official SDKs).
    - Ensure automatic reconnection logic and heartbeats (mandatory for Deribit).
    - Map raw WS messages to the standardized `MarketData` schema.
  </action>
  <verify>
    - Run self-test blocks in `data_feed.py` that connect to public streams (e.g., btcusdt@kline_1m) and print receipt status.
  </verify>
  <done>
    - Logic successfully receives 5+ messages from each exchange without disconnects.
  </done>
</task>

<task type="auto">
  <name>Update Feed Aggregator for Push Logic</name>
  <files>
    - app/core/feed_aggregator.py
  </files>
  <action>
    - Modify `FeedAggregator` to support `add_push_source()`.
    - Ensure that WebSocket messages trigger `MARKET_UPDATE` on the `EventBus` immediately.
  </action>
  <verify>
    - Run `python -m app.core.feed_aggregator` with mock WS pushes.
  </verify>
  <done>
    - Aggregator correctly broadcasts pushed updates to the system.
  </done>
</task>

## Success Criteria
- [ ] Real-time OHLCV data flowing from Binance/Deribit.
- [ ] Zero polling in core data ingestion path.
