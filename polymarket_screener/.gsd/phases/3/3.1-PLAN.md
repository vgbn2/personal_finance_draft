---
phase: 3
plan: 3.1
wave: 1
---

# Plan 3.1: Backtesting & Monte Carlo Stress Testing

## Objective
Implement the validation loop and randomized stress testing.

## Tasks

<task type="auto">
  <name>Backtesting Engine Loop</name>
  <files>backtest/engine.py, backtest/broker.py</files>
  <action>Implement time-series replay with slippage execution.</action>
  <verify>PnL log verification.</verify>
  <done>PnL calculated correctly.</done>
</task>

```python
# CODE SKETCH: Time-Series Replay Loop
def run_backtest(strategy, data):
    for row in data:
        signal = strategy.on_tick(row)
        if signal:
            broker.execute(signal, price=row.price)
    return broker.report()
```

<task type="auto">
  <name>Monte Carlo & Black Swan Stress</name>
  <files>backtest/monte_carlo.py</files>
  <action>Implement randomized shock injection for robustness.</action>
  <verify>Worst-case output test.</verify>
  <done>VaR metrics calculated.</done>
</task>

```python
# CODE SKETCH: Black Swan Shock Injection
def run_simulation(base_returns):
    sim = base_returns.copy()
    if random.random() < 0.05: # 5% chance of Black Swan
        sim[random_index] *= 0.5 # 50% crash
    return calculate_drawdown(sim)
```

## Success Criteria
- [ ] Robustness validated against crashes.
- [ ] Simulation covers multiple iterations.
