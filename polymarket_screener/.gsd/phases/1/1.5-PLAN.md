---
phase: 1
plan: 5
wave: 3
---

# Plan 1.5: Core Models & Strict Schemas (Pydantic)

## Objective
Prevent "Poisoned JSON" and bad data types from crashing the math modules or databases. This serves as the fundamental type-enforcement layer for the entire application.

## Context
- [MASTERPLAN.md](file:///c:/Users/Lenovo/Desktop/VGBN/.vscode/CODEPTIT/polymarket_screener/.gsd/phases/docs/MASTERPLAN.md)
- `mass_audit_report.md` (Schema Enforcement Suggestion)

## Tasks

<task type="auto">
  <name>Implement Pydantic Data Models</name>
  <files>
    - data/models.py
  </files>
  <action>
    Define strict `Pydantic` models for:
    - `MarketSnapshot` (Spot price, Best Bid/Ask, Timestamp)
    - `OrderbookState` (Bids Array, Asks Array)
    - `TradeSignal` (Target Price, Target Side, Allocation, Expected ROI)
    - `ExecutionResult` (Filled Price, Gas Cost, Transaction Hash)
    Ensure all models validate datetime fields and coerce floats properly.
  </action>
  <verify>python tests/test_models.py</verify>
  <done>Models initialize correctly and reject invalid/missing required fields.</done>
</task>

## Success Criteria
- [ ] No `KeyError` or `TypeError` crashes can occur when parsing valid Polymarket/Binance JSON responses.
- [ ] All timestamps are standardized to UTC.
