---
phase: 1
plan: 4
wave: 3
---

# Plan 1.4: Async Data Aggregation & Storage

## Objective
The "Nervous System." Uses the Hyperglycemia Event Bus pattern to fuse Polymarket, Binance, and Deribit into a single, synchronized `MarketState`.

## Context
- [FULL_CODE_SPEC.md](file:///C:/Users/Lenovo/.gemini/antigravity/brain/571bca6a-8901-4fac-b1a0-fa88d6b509cf/full_code_spec.md)
- Hyperglycemia `coordinator.py` (Event Bus blueprint)

## Tasks

<task type="auto">
  <name>Implement Data Aggregator & Sync Logic</name>
  <files>
    - data/data_aggregator.py
  </files>
  <action>
    1. Implement the `DataAggregator` with an `asyncio.Queue` event bus.
    2. Write the `sync_worker()` to process incoming ticks from multiple exchanges.
    3. Implement "Stale Data Poisoning" protection: If a Binance tick is older than 200ms or a Polymarket tick is older than 5s relative to the other, the update is flagged as `STALE`.
  </action>
  <verify>python tests/test_aggregator.py</verify>
  <done>Aggregator successfully aligns a Binance BTC price with a Polymarket YES-BTC price.</done>
</task>

<task type="auto">
  <name>Implement Parquet Local Caching</name>
  <files>
    - data/storage.py
  </files>
  <action>
    Use `pandas` and `fastparquet` to implement a high-speed `TimeSeriesStore`. 
    Every 1,000 market ticks, the buffer should flush to the local `data/cache/` directory.
    This is essential for the "Zero-API-Wait" backtesting in Phase 3.
  </action>
  <verify>ls data/cache/*.parquet</verify>
  <done>System generates valid Parquet files during a 5-minute ingestion test.</done>
</task>

## Success Criteria
- [ ] The aggregator handles 100+ ticks/second without CPU spike.
- [ ] Parquet files are readable and contain accurate timestamps.
