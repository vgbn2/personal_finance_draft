---
phase: 1
plan: 3
wave: 2
---

# Plan 1.3: Polymarket Data Clients (High-Fidelity)

## Objective
Implement the specialized clients for Polymarket's dual-API system (Gamma for metadata, CLOB for orderbooks). These will use the "Rate-Limited" base from Plan 1.2.

## Context
- [FULL_CODE_SPEC.md](file:///C:/Users/Lenovo/.gemini/antigravity/brain/571bca6a-8901-4fac-b1a0-fa88d6b509cf/full_code_spec.md)
- `_research/macroe/polymarket_client.py` (Legacy source)

## Tasks

<task type="auto">
  <name>Implement Gamma Metadata Client</name>
  <files>
    - api/clients/gamma_client.py
  </files>
  <action>
    Implement `GammaClient` with `fetch_active_slugs()` and `get_market_metadata()`.
    Use the `15min_window_epoch` logic to automatically discover crypto-event markets.
    Ensure it extracts `clobTokenIds` correctly from the nested API response.
  </action>
  <verify>python tests/test_gamma_client.py</verify>
  <done>Gamma client can fetch and parse at least 10 active crypto markets.</done>
</task>

<task type="auto">
  <name>Implement CLOB Orderbook Client</name>
  <files>
    - api/clients/clob_client.py
  </files>
  <action>
    Implement `ClobClient` with a focus on `get_orderbook(token_id)`.
    Crucial: Implement `calculate_vwap(token_id, size_usd)` as per Roan's research. It must account for slippage when traversing the orderbook depth.
  </action>
  <verify>python tests/test_clob_client.py</verify>
  <done>CLOB client returns correct VWAP for a $1,000 order versus the current book.</done>
</task>

## Success Criteria
- [ ] Clients handle 429 and 504 errors without crashing the main thread.
- [ ] VWAP math is validated against manual orderbook snapshots.
