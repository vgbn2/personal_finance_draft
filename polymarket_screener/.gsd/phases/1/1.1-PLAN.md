---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Modular Foundation & Utilities

## Objective
Initialize the `app/` package structure and implement the foundational utilities (Config, Logger, Types) required for a scalable and decentralized system. This ensures that all subsequent modules have access to a unified configuration and logging interface.

## Context
- .gsd/phases/docs/SPEC.md
- .gsd/phases/docs/ROADMAP.md
- app/ (New)

## Tasks

<task type="auto">
  <name>Initialize app/ Package Structure</name>
  <files>
    - app/__init__.py
    - app/core/__init__.py
    - app/execution/__init__.py
    - app/math/__init__.py
    - app/api/__init__.py
    - app/utils/__init__.py
  </files>
  <action>
    Create the root `app/` directory and its sub-packages. Ensure each directory contains an `__init__.py` file to enable absolute imports (e.g., `from app.utils.config import Config`).
  </action>
  <verify>ls -R app/ | grep __init__.py</verify>
  <done>Full directory tree exists with __init__.py files.</done>
</task>

<task type="auto">
  <name>Implement Centralized Config and Logger</name>
  <files>
    - app/utils/config.py
    - app/utils/logger.py
    - app/utils/types.py
  </files>
  <action>
    1. Implement `app/utils/config.py` using `PyYAML` and `Pydantic` to load `config/strategy_params.yaml` and `config/symbols.yaml`.
    2. Implement `app/utils/logger.py` for structured logging (Rich for console, JSON for file/cloud).
    3. Define shared base Pydantic models in `app/utils/types.py` for API data integrity.
  </action>
  <verify>python -c "from app.utils.config import StrategyConfig; from app.utils.logger import setup_logger"</verify>
  <done>Utilities are functional and pass basic import and execution tests.</done>
</task>

```python
# CODE SKETCH: Centralized Config (app/utils/config.py)
from pydantic import BaseModel, ConfigDict
import yaml

class StrategyConfig(BaseModel):
    min_edge: float = 0.04
    
    @classmethod
    def load(cls, path: str = "config.yaml"):
        with open(path, "r") as f:
            return cls(**yaml.safe_load(f))
```

<task type="auto">
  <name>Internal Event Bus (PubSub)</name>
  <files>app/core/event_bus.py</files>
  <action>
    Implement a lightweight cross-module Event Bus using `asyncio.Queue`.
    Define channels like `MARKET_UPDATE`, `WINDOW_ROLLED`, and `SIGNAL_DETECTED`.
  </action>
  <verify>python -c "import app.core.event_bus"</verify>
  <done>EventBus class is capable of publish/subscribe message routing.</done>
</task>

```python
# CODE SKETCH: Async Event Bus
import asyncio

class EventBus:
    def __init__(self):
        self._subscribers = {}

    def subscribe(self, channel: str, queue: asyncio.Queue):
        if channel not in self._subscribers:
            self._subscribers[channel] = []
        self._subscribers[channel].append(queue)

    async def publish(self, channel: str, message: any):
        if channel in self._subscribers:
            for queue in self._subscribers[channel]:
                await queue.put(message)
```

## Success Criteria
- [ ] Absolute imports work throughout the `app/` package.
- [ ] No hardcoded configuration exists outside of YAML files.
- [ ] Logging is centralized and follows a consistent format.
