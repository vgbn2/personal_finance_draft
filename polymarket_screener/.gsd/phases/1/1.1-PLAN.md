---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Project Foundation & Symbol Registry

## Objective
Establish the "Not Broken" directory structure and a decoupled symbol registry system. This ensures that the engine never hardcodes asset identifiers, making it trivial to add new Polymarket or Binance pairs later.

## Context
- [CONTEXT.md](file:///c:/Users/Lenovo/Desktop/VGBN/.vscode/CODEPTIT/polymarket_screener/CONTEXT.md)
- [INFRASTRUCTURE.md](file:///c:/Users/Lenovo/Desktop/VGBN/.vscode/CODEPTIT/polymarket_screener/INFRASTRUCTURE.md)
- [symbols.yaml](file:///c:/Users/Lenovo/Desktop/VGBN/.vscode/CODEPTIT/polymarket_screener/config/symbols.yaml)

## Tasks

<task type="auto">
  <name>Initialize Directory Structure</name>
  <files>
    - config/
    - data/
    - strategy/
    - validation/
    - execution/
    - api/
    - tests/
  </files>
  <action>
    Create the core module directories with `__init__.py` files to ensure proper Python package resolution. 
    Strictly follow the structure defined in CONTEXT.md.
  </action>
  <verify>ls -R | grep __init__.py</verify>
  <done>All core directories exist and contain __init__.py files.</done>
</task>

<task type="auto">
  <name>Implement Decoupled Symbol Registry</name>
  <files>
    - config/config.py
    - config/symbols.yaml
  </files>
  <action>
    Write a `Config` class in `config/config.py` that uses `PyYAML` to parse `symbols.yaml`. 
    The registry should map internal IDs (e.g., 'POLY-BTC') to exchange-specific identifiers (Gamma Slugs/CLOB Token IDs).
    Include a `SymbolRegistry` helper that provides a unified interface for the rest of the application.
  </action>
  <verify>python -c "from config.config import registry; print(registry.get_all_symbols())"</verify>
  <done>symbols.yaml is parsed correctly and the registry returns valid asset mappings.</done>
</task>

## Success Criteria
- [ ] Directory structure is clean and PEP8 compliant.
- [ ] No hardcoded strings for assets exist in the foundation code.
