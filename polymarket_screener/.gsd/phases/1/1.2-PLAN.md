---
phase: 1
plan: 1.2
wave: 1
---

# Plan 1.2: VPN-Safe Streams & Rate-Limited REST

## Objective
Implement WebSocket streams and REST clients with protection against rate-limits and VPN networking issues.

## Tasks

<task type="auto">
  <name>VPN-Safe Server & WS Bridge</name>
  <files>api/server.py, api/ws_bridge.py</files>
  <action>
    - Configure FastAPI to bind to `0.0.0.0` to allow VPN-local access.
    - Implement a WebSocket bridge that fans out data to the frontend.
  </action>
  <verify>Check server binding on 0.0.0.0.</verify>
  <done>Server is accessible over VPN.</done>
</task>

```python
# CODE SKETCH: VPN-Safe FastAPI Binding
app = FastAPI()
if __name__ == "__main__":
    uvicorn.run(app, host="0.0.0.0", port=8000) # Bind to all interfaces for VPN friendliness

# CODE SKETCH: WS Bridge Logic
class WSBridge:
    def __init__(self, event_bus):
        self.clients = set()
        event_bus.subscribe(self.broadcast)
    async def broadcast(self, data):
        for client in self.clients:
            await client.send_json(data)
```

<task type="auto">
  <name>Rate-Limited REST Clients</name>
  <files>data/utils/rate_limiter.py, data/clients/stock_options_client.py</files>
  <action>Implement centralized throttling to avoid 429 errors from Stocks/Crypto providers.</action>
  <verify>Stress test client throttling.</verify>
  <done>Limits respected.</done>
</task>

```python
# CODE SKETCH: Centralized Throttling
class RateLimiter:
    def __init__(self, calls_per_sec):
        self.delay = 1.0 / calls_per_sec
        self.last_call = 0
    def wait(self):
        elapsed = time.time() - self.last_call
        if elapsed < self.delay:
            time.sleep(self.delay - elapsed)
        self.last_call = time.time()
```

## Success Criteria
- [ ] API is openable over VPN.
- [ ] Data ingestion respects provider limits.
