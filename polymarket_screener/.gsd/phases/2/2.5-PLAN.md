---
phase: 2
plan: 5
wave: 1
depends_on: []
files_modified: ["app/utils/config.py", "app/core/aggregator.py"]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Config models include base_currency, quote_currency, and risk thresholds."
    - "Aggregator uses config.base_currency instead of hardcoded 'BTC'."
  artifacts:
    - "app/utils/config.py updated with new Pydantic fields."
    - "app/core/aggregator.py refactored for dynamic symbols."
---

# Plan 2.5: Foundation Refactor (Dynamic Symbols & Config)

<objective>
Generalize the configuration system and data aggregator to support any asset (BTC, ETH, etc.) and move magic numbers to the config layer.

Purpose: Decouple the bot from BTC-specific logic.
Output: Flexible configuration and generalized data polling.
</objective>

<context>
Load for context:
- app/utils/config.py
- app/core/aggregator.py
</context>

<tasks>

<task type="auto">
  <name>Update Config Models</name>
  <files>app/utils/config.py</files>
  <action>
    Add following fields:
    - AppConfig: `base_currency: str = "BTC"`, `quote_currency: str = "USDT"`
    - StrategyConfig: `default_dte_minutes: int = 15`, `vrp_discount_factor: float = 0.85`
    - RiskConfig: `max_global_exposure_pct: float = 0.30`, `max_temporal_exposure_pct: float = 0.15`
    AVOID: Changing existing field names to prevent breaking existing YAML loads.
  </action>
  <verify>python -m app.utils.config (verify no pydantic validation errors)</verify>
  <done>Pydantic models include the new fields with correct defaults.</done>
</task>

<task type="auto">
  <name>Generalize Data Aggregator</name>
  <files>app/core/aggregator.py</files>
  <action>
    - Replace hardcoded "BTC" and "BTC/USDT" with `config_manager.config.base_currency`.
    - Update `_check_staleness` to use thresholds from config if available (otherwise fallback to existing constants).
    AVOID: Breaking the concurrent gather logic.
  </action>
  <verify>$env:PYTHONPATH="."; python -m app.core.aggregator</verify>
  <done>Aggregator fetch logic uses dynamic symbol from config.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Config manager loads with new fields.
- [ ] Aggregator successfully initializes with dynamic base currency.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
