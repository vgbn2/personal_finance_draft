---
phase: 2
plan: 3
wave: 2
---

# Plan 2.3: Orderbook Slippage & Dashboard API

## Objective
Finalize the slippage math integration from the CLOB client and expose the current strategy state (Greeks, positions, signals) to the frontend via the threaded FastAPI.

## Context
- [MASTERPLAN.md](file:///c:/Users/Lenovo/Desktop/VGBN/.vscode/CODEPTIT/polymarket_screener/.gsd/phases/docs/MASTERPLAN.md)
- `data/clients/clob_client.py`

## Tasks

<task type="auto">
  <name>Implement VWAP Slippage Calculator</name>
  <files>
    - strategy/slippage.py
  </files>
  <action>
    Implement `calculate_vwap` that takes an `OrderbookState` (from Plan 1.5 Pydantic schema) and a target `allocation_usd`.
    It must walk down the orderbook depth until the allocation is filled, returning the effective price.
  </action>
  <verify>python tests/test_slippage.py</verify>
  <done>Slippage correctly returns higher prices for large allocations against thin orderbooks.</done>
</task>

<task type="auto">
  <name>Build Frontend State API</name>
  <files>
    - api/routes/state.py
  </files>
  <action>
    Create FastAPI endpoints (`/api/state`) that summarize the `Portfolio` class.
    Return JSON matching the frontend's expected schema (active markets, current open probabilities, global exposure).
  </action>
  <verify>curl http://127.0.0.1:8000/api/state</verify>
  <done>API returns valid JSON with current exposure and market states.</done>
</task>

## Success Criteria
- [ ] VWAP correctly throws `InsufficientLiquidity` if the book is too thin.
- [ ] `/api/state` endpoint responds in < 50ms.
