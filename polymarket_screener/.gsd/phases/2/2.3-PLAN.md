---
phase: 2
plan: 3
wave: 2
---

# Plan 2.3: Orderbook Slippage & Dashboard API

## Objective
Finalize the slippage math integration from the CLOB client and expose the current strategy state (Greeks, positions, signals) to the frontend via the threaded FastAPI.

## Context
- [MASTERPLAN.md](file:///c:/Users/Lenovo/Desktop/VGBN/.vscode/CODEPTIT/polymarket_screener/.gsd/phases/docs/MASTERPLAN.md)
- `data/clients/clob_client.py`

## Tasks

<task type="auto">
  <name>Implement Global Window Sequencer</name>
  <files>
    - data/window_sequencer.py
    - strategy/market_screener.py
  </files>
  <action>
    1. Implement `data/window_sequencer.py` as a global heartbeat service.
    2. Ensure it calculates the next market title (e.g., "9:30" -> "9:45") and forces the Screener to subscribe to the new ID.
    3. Add a "Watchdog" that alerts if the next market isn't found 60s before expiry.
  </action>
  <verify>python tests/test_global_rolling.py</verify>
  <done>Entire system (Data Feed + Screener) transitions to the 10:00 window ID automatically when 9:45 ends.</done>
</task>

<task type="auto">
  <name>Implement Settlement Monitor & Vacuuming</name>
  <files>
    - execution/portfolio.py
    - execution/settlement.py
  </files>
  <action>
    1. Update `Portfolio` to handle `active` vs `settled` positions.
    2. Implement `settlement.py` to poll Polymarket for resolution status after expiry.
    3. Ensure cash balance is updated and expired records are vacuumed from active memory.
  </action>
  <verify>python tests/test_auto_resolution.py</verify>
  <done>Portfolio correctly settles a "Won" trade and recycles the capital into the cash balance automatically.</done>
</task>

## Success Criteria
- [ ] System automatically transitions recurring 15m windows without manual intervention.
- [ ] VWAP correctly throws `InsufficientLiquidity` if the book is too thin.
- [ ] `/api/state` endpoint responds in < 50ms.
