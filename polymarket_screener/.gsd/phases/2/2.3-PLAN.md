---
phase: 2
plan: 2.3
wave: 2
---

# Plan 2.3: Orderbook Slippage & Dashboard API

## Objective
Enhance `clob_client.py` with a Slippage & Impact calculator and implement the `live/api.py` foundation using a threaded Flask server (Terminus pattern) for local monitoring.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- data/polymarket/clob_client.py

## Tasks

<task type="auto">
  <name>Slippage & Impact Calculator</name>
  <files>
    data/polymarket/clob_client.py
  </files>
  <action>
    - Add a method `calculate_slippage(token_id, size_usd)` to `ClobClient`.
    - It should traverse the orderbook to find the average execution price for a given size.
    - Return `effective_price` and `slippage_percentage`.
  </action>
  <verify>Run a test script for a specific token_id with varying sizes ($100, $1000) and print slippage.</verify>
  <done>Slippage calculation accurately reflects the depth of the Polymarket orderbook.</done>
</task>

<task type="auto">
  <name>Threaded Dashboard API</name>
  <files>
    live/api.py
  </files>
  <action>
    - Implement a Flask application in `live/api.py`.
    - Create a `start_api()` function that runs the app on a local port (e.g., 5001) without `use_reloader`.
    - Add an endpoint `/api/state` that returns a mocked JSON of the current strategy state.
  </action>
  <verify>Run `live/api.py` and access `http://127.0.0.1:5001/api/state` in the browser or via `curl`.</verify>
  <done>The API server runs independently and serves JSON data.</done>
</task>

## Success Criteria
- [ ] Orderbook depth is factored into price calculations.
- [ ] A local API server is ready to bridge the backend to a future frontend dashboard.
