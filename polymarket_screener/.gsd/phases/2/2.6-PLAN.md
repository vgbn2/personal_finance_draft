---
phase: 2
plan: 6
wave: 2
depends_on: ["2.5"]
files_modified: ["app/math/black_scholes.py", "app/math/kelly.py", "app/execution/risk.py", "app/core/screener.py"]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "RiskManager uses dynamic CONVICTION_TIERS from config."
    - "BlackScholes singleton uses vrp_discount from config."
    - "Screener uses dynamic DTE from config window."
  artifacts:
    - "app/execution/risk.py refactored for dynamic gates."
    - "app/math/kelly.py defaults mapped to config."
---

# Plan 2.6: Math & Risk Parameterization

<objective>
Refactor mathematical models and risk gate implementation to pull coefficients and thresholds from ConfigManager.

Purpose: Enable real-time tuning of risk and math without code deploys.
Output: Decoupled risk engine and math layer.
</objective>

<context>
Load for context:
- app/utils/config.py
- app/execution/risk.py
- app/math/black_scholes.py
- app/core/screener.py
</context>

<tasks>

<task type="auto">
  <name>Refactor Risk Gates</name>
  <files>app/execution/risk.py</files>
  <action>
    - Replace static `CONVICTION_TIERS` dict with `config_manager.risk.conviction_tiers`.
    - Update `RiskManager.__init__` to default `max_global_exposure` and `max_temporal_exposure` from `config_manager`.
    AVOID: Breaking the `get_conviction_cap` lookup logic.
  </action>
  <verify>$env:PYTHONPATH="."; python -m app.execution.risk</verify>
  <done>RiskManager initializes with config-driven thresholds.</done>
</task>

<task type="auto">
  <name>Parameterize Math & Screener</name>
  <files>app/math/black_scholes.py, app/core/screener.py</files>
  <action>
    - Update `BlackScholes` __init__ to pull `vrp_discount` from `config_manager`.
    - Update `MarketScreener` to calculate `dte_days` using `config_manager.strategy.default_dte_minutes`.
    - Ensure Kelly fraction (0.25) is pulled from `config_manager.strategy.kelly_fraction`.
    AVOID: Hardcoding "15" anywhere in the screener logic.
  </action>
  <verify>$env:PYTHONPATH="."; python -m app.core.screener</verify>
  <done>Screener and Math layers are fully configuration-driven.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Risk engine rejects orders when config thresholds are lowered.
- [ ] Screener fair price changes when `vrp_discount` is modified in YAML.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
