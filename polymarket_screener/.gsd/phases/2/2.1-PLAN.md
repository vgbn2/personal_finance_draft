---
phase: 2
plan: 2.1
wave: 1
---

# Plan 2.1: Market Screener

## Objective
Implement `market_screener.py` to filter actionable Polymarket events based on custom criteria such as liquidity, volume, and time to resolution.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- config/strategy_params.json

## Tasks

<task type="auto">
  <name>Implement MarketScreener</name>
  <files>
    screener/market_screener.py
  </files>
  <action>
    - Define a `MarketScreener` class that loads parameters from `config/strategy_params.json`
    - Implement a method `filter_events(events: list) -> list` that filters events based on volume, liquidity, or end date thresholds.
    - Avoid hardcoding specific event IDs; make it generic based on criteria.
  </action>
  <verify>Run a Python script importing `MarketScreener` and instantiating it without errors.</verify>
  <done>The `MarketScreener` class exists and can filter a mocked list of events successfully.</done>
</task>

## Success Criteria
- [ ] `MarketScreener` filters out events that do not meet the criteria in strategy parameters.
- [ ] The class is independent and can be tested with mock data.
