---
phase: 2
plan: 2.1
wave: 1
---

# Plan 2.1: Market Screener

## Objective
Implement `market_screener.py` to easily select specific core assets (e.g. BTC, ETH) or filter distinct event types (e.g. tweet markets, elections), and rank them based on custom criteria such as liquidity, volume, and time to resolution.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- config/strategy_params.json

## Tasks

<task type="auto">
  <name>Implement MarketScreener</name>
  <files>
    screener/market_screener.py
  </files>
  <action>
    - Define a `MarketScreener` class that loads parameters from `config/strategy_params.json`.
    - Implement methods to isolate specific asset categories or event keywords (e.g. "tweet").
    - Implement a method `filter_events(events: list) -> list` that filters events based on volume, liquidity, or end date thresholds specific to that asset/event configuration.
  </action>
  <verify>Run a Python script importing `MarketScreener` and filtering mocked assets/events.</verify>
  <done>The `MarketScreener` class exists and correctly isolates a required subset of events according to the config parameters.</done>
</task>

## Success Criteria
- [ ] `MarketScreener` effectively identifies and isolates specific asset or event categories.
- [ ] Events that do not meet custom liquidity/volume criteria are correctly filtered.
- [ ] The class successfully relies on configurations loaded separately for each event type.
