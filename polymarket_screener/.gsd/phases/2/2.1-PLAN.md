---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Stateless Quant Math (Black-Scholes & Greeks)

## Objective
Implement the pure, stateless mathematical core of the engine. This defines the "Fair Price" for every Polymarket outcome using vectorized NumPy operations to keep CPU usage low.

## Context
- [logical_requirements.md](file:///c:/Users/Lenovo/Desktop/VGBN/.vscode/CODEPTIT/polymarket_screener/docs/logical_requirements.md)
- `old317/bot_eth/quant_engine.py` (Source of `calculate_nd2`)

## Tasks

<task type="auto">
  <name>Implement Vectorized Black-Scholes Engine</name>
  <files>
    - logic/quant_math.py
  </files>
  <action>
    Create a `QuantMath` module. 
    1. Implement `calculate_nd2` (Normal CDF for d2).
    2. Crucial: Use `@np.vectorize` or direct NumPy array math to allow calculating probabilities for 100+ strikes in a single caller cycle.
    3. Include `calculate_iv_haircut` to apply the VRP discount.
  </action>
  <verify>python tests/test_quant_math.py</verify>
  <done>Engine calculates correct probabilities for both scalar and array inputs.</done>
</task>

<task type="auto">
  <name>Implement Market Screener & Window Rolling</name>
  <files>
    - strategy/market_screener.py
    - strategy/window_sequencer.py
  </files>
  <action>
    1. Build the screener to filter markets by volume/asset.
    2. Implement `window_sequencer.py` to parse market titles (e.g., "BTC Price 9:30") and calculate the next epoch.
    3. Ensure the screener "pre-warms" the next window 5 minutes before expiry.
  </action>
  <verify>python tests/test_window_rolling.py</verify>
  <done>Screener automatically identifies and subscribes to the 10:00 window when the 9:45 window is nearing its end.</done>
</task>

<task type="auto">
  <name>Implement Greed-Decay TP Logic</name>
  <files>
    - logic/lifecycle.py
  </files>
  <action>
    Implement the `GreedDecay` class based on the formula: `target = p_real * exp(-k * ROI)`.
    Ensure `k` is dynamically calculated based on the token's entry price ($0.03 vs $0.80).
  </action>
  <verify>python tests/test_lifecycle.py</verify>
  <done>TP target decreases as ROI increases, exactly as specified in the logic spec.</done>
</task>

## Success Criteria
- [ ] No Python loops are used for probability calculations.
- [ ] Logic perfectly matches the `logical_requirements.md` blueprint.
