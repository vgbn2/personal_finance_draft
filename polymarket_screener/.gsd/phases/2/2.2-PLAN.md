---
phase: 2
plan: 2
wave: 1
---

# Plan 2.2: Frank-Wolfe Kelly Optimizer

## Objective
Implement the portfolio optimization engine. This ensures that even if 10 markets appear "profitable," the bot only allocates capital to the most efficient combination of strikes.

## Context
- [logical_requirements.md](file:///c:/Users/Lenovo/Desktop/VGBN/.vscode/CODEPTIT/polymarket_screener/docs/logical_requirements.md)
- `old317/bot_eth/quant_engine.py` (Source of `frank_wolfe_optimizer`)

## Tasks

<task type="auto">
  <name>Implement Frank-Wolfe Convex Optimizer</name>
  <files>
    - logic/optimizer.py
  </files>
  <action>
    Port the `frank_wolfe_optimizer` from Old 317. 
    1. Implement `build_state_probabilities` (N+1 states).
    2. Implement `build_returns_matrix` (Asset returns across all states).
    3. Implement the iterative FW loop with a `max_iter=5000` and `tol=1e-6`.
  </action>
  <verify>python tests/test_optimizer.py</verify>
  <done>Optimizer generates weights that sum to <= 1.0 (with cash asset) and favors high-edge assets.</done>
</task>

<task type="auto">
  <name>Implement 3D Risk Matrix Gates</name>
  <files>
    - logic/risk_manager.py
  </files>
  <action>
    Create a `RiskManager` class with three `check_gate()` methods:
    - `check_global_exposure()`: 30% cap.
    - `check_date_correlation()`: 15% cap per expiry date.
    - `check_conviction_tier()`: Tiered sizing based on win probability.
  </action>
  <verify>python tests/test_risk_manager.py</verify>
  <done>Risk manager correctly blocks orders that exceed any of the three gates.</done>
</task>

## Success Criteria
- [ ] Optimizer handles zero-edge or negative-edge scenarios without crashing.
- [ ] Risk Matrix is "Strict" (No order can bypass a closed gate).
