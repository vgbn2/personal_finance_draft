---
phase: 2
plan: 2.2
wave: 1
---

# Plan 2.2: BaseStrategy Interface

## Objective
Define the core `BaseStrategy` interface in `strategy/base.py` that all future trading strategies will inherit from.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md

## Tasks

<task type="auto">
  <name>Implement BaseStrategy</name>
  <files>
    strategy/base.py
  </files>
  <action>
    - Create an abstract base class `BaseStrategy` or a base class with `NotImplementedError` for core methods.
    - Required methods to define:
      - `__init__(self, config)`
      - `on_market_event(self, event_data)`: Called when Polymarket data updates.
      - `on_macro_data(self, macro_data)`: Called when macro data updates.
      - `calculate_signals(self)`: Generates trading signals.
  </action>
  <verify>Run a Python script importing `BaseStrategy` and verifying its required methods.</verify>
  <done>The `BaseStrategy` class is defined with all required lifecycle method signatures.</done>
</task>

## Success Criteria
- [ ] `BaseStrategy` outlines all required methods for a compatible strategy.
- [ ] A dummy subclass can inherit from it without syntax errors.
