---
phase: 2
plan: 4
wave: 2
---

# Plan 2.4: Execution Safety, Sync & Alerts

## Objective
Implement the "Zero-Break" operational components: Execution sequence locking, startup state recovery, and Telegram push notifications.

## Context
- `old317/bot_eth/bot.py` (Source of `asyncio.Lock` and `_resolve_expired`)
- `hyperglycemia-faint-predictor/src/alerts/controller.py` (Source of Telegram alerts)

## Tasks

<task type="auto">
  <name>Implement Execution Locks & Broker</name>
  <files>
    - execution/broker.py
  </files>
  <action>
    Create the `Broker` class.
    Implement an `asyncio.Lock()` specifically for the `execute_trade()` function. 
    This prevents Polymarket Nonce collisions if the optimizer tries to spam 5 orders in the exact same millisecond.
  </action>
  <verify>python tests/test_broker_locks.py</verify>
  <done>Broker properly queues simultaneous trade requests instead of executing them concurrently.</done>
</task>

<task type="auto">
  <name>Implement State Recovery & Alerts</name>
  <files>
    - execution/reconciliation.py
    - utils/alert_service.py
  </files>
  <action>
    1. Create `reconciliation.py` with an `on_startup()` routine that pulls persistent open positions to re-populate the live `Portfolio` state.
    2. Create `alert_service.py` to push non-blocking asynchronous notifications to Telegram (using a `TELEGRAM_BOT_TOKEN` from `.env`) when a Circuit Breaker activates or a trade fills.
  </action>
  <verify>python tests/test_reconciliation.py</verify>
  <done>Engine can reconstruct a valid Portfolio state from a mock database response.</done>
</task>

## Success Criteria
- [ ] Broker rejects/queues concurrent blockchain transactions safely.
- [ ] Bot can accurately resume trading logic with existing positions after a simulated process kill.
