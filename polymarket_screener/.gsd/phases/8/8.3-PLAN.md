---
phase: 8
plan: 3
wave: 2
---

# Plan 8.3: Multi-Market Scalability

## Objective
Refactor the signal and ingestion pipeline to handle dozens of concurrent markets without linear memory growth or blocking.

## Context
- app/core/signal_engine.py
- app/core/data_feed.py

## Tasks

<task type="auto">
  <name>Refactor Screener for Concurrency</name>
  <files>
    - app/core/signal_engine.py
  </files>
  <action>
    - Convert `SignalEngine.process()` to use `asyncio.gather` for independent market analysis.
    - Implement a "Market Ranker" if CPU bound (prioritize high-liquidity markets).
  </action>
  <verify>
    - Load test with 50 mock markets; measure processing time < 100ms.
  </verify>
  <done>
    - Sub-100ms latency on 50+ concurrent markets.
  </done>
</task>

<task type="auto">
  <name>Connection Pooling for Data Feeds</name>
  <files>
    - app/core/data_feed.py
  </files>
  <action>
    - Implement a single WebSocket connection per exchange that multiplexes all subscribed symbols into one stream.
  </action>
  <verify>
    - Confirm only one TCP connection is open to Binance while streaming 10 symbols.
  </verify>
  <done>
    - Efficient network usage via stream multiplexing.
  </done>
</task>

## Success Criteria
- [ ] System handles 50+ concurrent prediction markets.
- [ ] Low-latency execution path maintained under load.
