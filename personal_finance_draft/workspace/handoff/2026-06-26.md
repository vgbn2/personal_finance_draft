# Handoff — 2026-06-26

## Session 61 — mass implement + ONNX ML signals (2 commits)

### What shipped

**`90beba31`** `fix(dashboard): remove 3 stale dev-review comments`
- `sovereign_dashboard.mjs:171` — "is this redundant?" on `backend universe` resolved (it isn't — distinct capability surface from `status`/`integrity`)
- `sovereign_dashboard.mjs:260` — polymarket history "doesnt work" removed (fixed s54)
- `sovereign_dashboard.mjs:267` — polymarket backtest "doesnt work" removed (fixed s54/55)

**`68d29f66`** `feat(ml): ONNX inference runner + real ML signals in bt + bias`

Core deliverable — Phase 3 of ML_SECTION_PLAN.md:

- **`shared/lib/ml/onnx_runner.js`** (new): lazy InferenceSession singletons for all 3 trained ONNX models (`xgboost_v1`, `logistic_v1`, `regime_classifier`). Feature imputation from hardcoded medians (feature_config.yaml contract). Returns `{direction, confidence, class_probs}`. `precomputeForFeatures()` batch-attaches `_onnxPred` to feature rows before the sync backtest loop runs.

- **`shared/lib/ml/models.js`**: new `onnxModelCandidates` array; `resolveModel()` checks them first; `MODEL_ALIASES` updated (`xgboost→xgboost_v1`, `lr/logistic→logistic_v1`); `ONNX_MODEL_NAMES` Set exported.

- **`backend/cli/commands/research/research.js`**: detects ONNX model name, awaits `precomputeForFeatures()` on `featureFrame.features` before the sync `runBacktest()` calls (shared row objects — precomputing once covers train/test/filtered splits).

- **`backend/cli/commands/research/bias.js`**: adds async `logistic_v1` prediction on the 1d bar's TA features (rsi, close, atrPct, sma20). Result gains `ml:{direction,confidence,model,class_probs}`; table shows it below Overall. Non-fatal if onnxruntime-node unavailable.

- Also committed the s59 test files that were on disk but never staged: `backend_bridge.test.js`, `trade_args_from_action_flag.test.js`.

**Verified:**
- `predict('logistic_v1', {rsi:65, close:50000})` → `down/90%` (real ONNX inference)
- `bias BTCUSDT --json --no-backfill` → `{aggregate.bias:"short", ml:{direction:"down",confidence:0.9028,model:"logistic_v1"}}`
- Suite: **645/643/0fail/2skip** (+14 tests). Hygiene clean.

**Ponytail activated:** `~/.claude/CLAUDE.md` now has the `/ponytail` trigger entry pointing to `~/.claude/skills/ponytail/commands/ponytail.toml`.

### Current BTC signal (2026-06-26 session 61)
TA bias: SHORT (unchanged from session 60). ML (logistic_v1): DOWN, 90% confidence. Aligned.

### Open gaps (next session priorities)
1. **`bt --strategy xgboost_v1` real smoke test** — verify the backtest pipeline produces non-zero trades end-to-end (needs market data loaded, wasn't run this session due to backfill time). The precompute path is wired; trades should appear.
2. **bias correlation** — `mcp__sovereign__get_correlation` not yet wired (deferred this session; MCP server availability unclear in current env)
3. **graphify-out refresh** — stale since session 55; defer until next meaningful change
4. **Gate.io market-order semantics** — empirical probe `index.ts:309-319` still pending
5. **Docs skill question** — user asked mid-session; recommendation: no new skill needed, `BOOTSTRAP.md` + existing `docs-sync` covers it. `docs/codebase_tour/` already exists. Worth checking if stale per session 59 triage notes.

### Boot notes for next session
- Suite baseline: **645/643/0fail/2skip** on `feat/ink-tui-refactor`
- ONNX models: `storage/models/{xgboost_v1,logistic_v1,regime_classifier}.onnx` — real, promoted, on disk
- `onnxruntime-node` is now in `package.json` — `npm install` needed on fresh clone
- `.mcp.json` at project root (gitignored) — mcp__sovereign__* tools auto-load
- `bias` command now includes `ml` field in JSON output; table shows ML signal row below Overall
