# Handoff — 2026-06-15

## Update - 2026-06-15 session 34 (continued from s33 context compaction) — dead-symbol gate + TUI/daemon polish

Boot on HEAD `a270eae6` (session-33 close-out docs). Branch `feat/session-guard-intraday-rollup`.
Suite 465/465 maintained throughout session.

**Session work (all previously committed in this context):**

Earlier in session 34 (before context compaction) the following were committed:
- `74b0ec67` — feat: backfill-daemon promoted to TUI Deep Backfill entry; rollup-priority guard in writeTsIndex (PROVIDER_PRIORITY: binance/alpaca=3, yahoo/twelvedata=1 — higher-priority bins block lower-priority overwrites; `derived_from_timeframe` stored and restored in ts/bin meta)
- `9b2fd784` — feat: `--concurrency` flag exposed in backfill-daemon CLI + TUI (default 5; 0 = per-lane defaults)
- `a57e392b` — refactor: daemon output simplified via `global.suppressLogs = true`; compact per-symbol lines (`→ DEEP 1825d`, `ok 10.2s`, `+5TF`); sub-command banners gated
- `f405263c` — feat: ts/bin freshness gate added to `ingestMarketData` for bulk OHLCV families (skips provider calls when bin is fresh + `force !== true`)
- `e0cb6aa2` — feat(coverage): dead-symbol gate — RNDRUSDT fix

**Dead-symbol gate detail (`e0cb6aa2`):**
- Problem: RNDRUSDT (delisted Binance symbol) returned 0 bars but `commandCryptoDeepBackfill` reported `ok:true`. Daemon's `decideAction` saw `count=0 → reason:'missing' → 'DEEP'` every cycle — infinite retry.
- Fix:
  - `coverage.js`: `readCoverage` detects meta-only `.meta.json` (no `.bin`) with `last_checked` field; `isFresh` returns `{ fresh: true, reason: 'not_found' }` for markers younger than `DEAD_SYMBOL_TTL_MS` (7 days)
  - `data.js` (`commandCryptoDeepBackfill`): `symbolOk = baseBars.length > 0` (was `|| snapErrors.length === 0`); writes meta marker `{ count:0, last_checked:now }` on 0-bar deep backfill
  - `backfill_daemon.js`: `decideAction` maps `fresh:true → 'skip'`; logs `✗ no data on provider (skip 7d)` for `not_found` skips
  - `coverage.test.js`: updated `deepEqual` assertion to include `notFoundCheckedMs: null`
- Verified: manual gate probe `{ fresh:true, reason:'not_found' }` + 465/465 suite exit 0

**Current HEAD:** `e0cb6aa2` | **Suite:** 465/465

**Next-session first step:** Ubuntu SSH unblock (user action needed):
1. Windows elevated PowerShell: `Start-Service sshd`
2. Ubuntu: `git fetch windows && git merge windows/feat/session-guard-intraday-rollup`
3. Ubuntu: `node backend/cli/sovereign_cli.js crypto-deep-backfill --days 1825` (multi-hour, Binance, keyless)

**Unchanged carryovers:**
- `graphify-out` refresh pending (status.js changed session 32, still deferred)
- FW2 monolith deconstruction
- FW6 backward-gap fetch
- merge `feat/ml-onnx-section` → main (user decision)
- live 1m provider smoke (Windows; needs network + Binance key)
- ~937MB untracked root artifacts (bundle files at `portable_exports/`)
- Ubuntu data gaps: crypto mostly `1d:1` (mass-backfill routes crypto via Yahoo; needs `crypto-deep-backfill`), equity `1m` absent (needs Alpaca key)

## Update - 2026-06-15 session 35 — blast-through deep pass: integrity 144× + marker fix + intraday-grain data repair

Boot on HEAD `e0cb6aa2` (`feat/session-guard-intraday-rollup`). Long session, **ALL CHANGES UNCOMMITTED**.
Suite **471/471** (was 465; +6 new tests across the session).

**1. Blast-through Focused Audit** (anchor `483d45cc`→`e0cb6aa2`): hygiene clean; one Medium finding —
the s34 dead-symbol marker clobbered an existing bin's enriched meta (proven via temp-dir probe).

**2. Fixes (code):**
- **`backend integrity` 57s → 0.4s (144×), output-identical.** Swapped the per-(symbol×tf) `readTsIndex`
  full-bin load for `readCoverage` (header + head/tail reads). Added `firstBarMs` to coverage.js. Proven
  equivalent over all 1009 real bins (0 mismatches) + adversarial edge-case test.
- **Marker clobber guard:** extracted exported `writeDeadSymbolMarker(tsDir,sym,tf,family,provider)` —
  writes the not-found marker ONLY when no `.bin` exists (a transient 0-bar fetch no longer strips
  coordinate_id/config_*/derived_from off real bars).
- **Dead export removed:** redundant `generatePolymarketFeatures` alias (real fn `buildPolymarketFeatureRows`
  stays). Bulk over-export prune (88) ATTEMPTED + REVERTED — names recur in internal object literals
  (e.g. `IndicatorMethods`), so line-removal corrupts internal state; needs AST. Logged as backlog.

**3. Intraday mixed-grain DATA REPAIR (user-reported via integrity output, user-authorized):**
- Coarse daily data had leaked into intraday bins (e.g. `CORN_15m` spanned 2002→2026 @ ~1.5 bars/day).
- Scan → 83 corrupt bins / 38 symbols: 9 commodity/metal (`15m`+some `4h` 2002 leak) + 13 orphan crypto
  alts (all-synthetic, not in active config) + 4 stray `1m:5` stubs.
- **Reversible fix:** quarantined corrupt bins to `storage/data/_quarantine_grain/` (8.3M, gitignored,
  MOVED not deleted); re-derived clean from deepest clean divisor (commodity 15m/4h←5m, VN 4h←1h).
  Re-scan: **0 corrupt**. CORN 15m now 3,733 real 15m bars; NG 4h medianGap 240min.
- **Guard added:** `isGrainSuspect()` in coverage.js (cheap span>2yr + low-density check), wired into
  `backend integrity` as advisory (`grain_suspect`/`total_grain_suspect` JSON + yellow line, non-gating).
  0 flagged across 941 live bins; tested.

**Changed (uncommitted) files:** `backend/cli/commands/data/data.js`, `backend/cli/commands/tools/backend.js`,
`shared/lib/market/coverage.js`, `shared/lib/market/polymarket_features.js`,
`tests/scripts/tests/coverage.test.js` (+grain/marker tests), NEW `dead_symbol_marker.test.js` +
`integrity_coverage_equivalence.test.js`, workspace docs. Data: rebuilt ts bins + `_quarantine_grain/`
(both gitignored, local-only).

**HEAD:** `e0cb6aa2` (nothing committed this session) | **Suite:** 471/471

**Next-session first step:** commit decision (suggested split: A perf/integrity, B fix/marker-guard,
C refactor/polymarket-alias, D feat/grain-guard). Then optionally tackle the **intraday depth
inconsistency** (separate from corruption — Yahoo TFs have different native depths, e.g. VCB 5m≈83d vs
1h≈508d; needs a network re-fetch pass, NOT done).

## Update - 2026-06-15 session 36 — backfill-daemon OOM fixed at the root + hard-tested + COMMITTED

User hit `backfill-daemon --once --concurrency 5` → `FATAL ERROR: JavaScript heap out of memory` (crypto
lane, ~4GB), twice. Root-caused, fixed at the root, hard-tested, refined the tests after user skepticism,
and committed (the session-35 batch above is now committed too).

**Root cause:** each crypto incremental job deserialized the entire multi-million-row 1m bin (BTCUSDT 1m
= 3.08M record objects, each w/ a fresh ISO string) **twice** — sink 1 = the merge-write in
`writeTsIndex` (read existing bin to merge-protect), sink 2 = `rollupFromBase` (read whole bin to derive
coarser TFs). At concurrency 3-5 across BTC/ETH/SOL → >4GB.

**Fix:**
- `validation.js` `mergeWriteBin`: `writeTsIndex` merges on the existing bin's **Buffer** (external mem,
  off-heap) via a two-sorted-stream merge — retained rows copied as 48-byte slices, only incoming rows
  become objects. Byte-identical to the old merge; also kills a latent `push(...existing)` RangeError.
  New `readTsIndexSince` (binary-search tail read).
- `data.js` `rollupFromBase(...,{sinceMs})`: incremental jobs re-derive only the recent UTC-day-aligned
  window (lossless).
- `backfill_daemon.js`: `LANE_MAX_CONCURRENCY={binance:3,alpaca:3}` clamps `--concurrency` on the 1m
  lanes; `docker-compose.yml` daemon `NODE_OPTIONS=--max-old-space-size=6144` (insurance).

**Hard test (user demanded "run it yourself"):** new `ts_merge_write.test.js` — byte-equiv (bin+meta) vs a
FROZEN reference transcription of the original merge + 3 real deep bins + a **child-process OOM
differential** (original child status 134 on a 1.3M-row bin under a 192MB cap, new child exit 0). After
user skepticism ("plan and fix those tests"), removed a durability bug (the test had used `git show HEAD`
as the golden ref, which breaks on commit) → frozen vendored reference + skip-safe git cross-check that
survives commit (proven: with git unavailable, 2 tests skip, 11 pass, 0 fail). **Live daemon at the stock
4GB heap** (the crashing config): 18/18 crypto, 0 errors, exit 0, 170s, peak RSS 2.68GB, ~3× faster;
post-run integrity clean (bins grew, no truncation). Suite **488/488**.

**Committed** on `feat/session-guard-intraday-rollup` (3 commits): (1) integrity/coverage/grain/polymarket
[s35 core], (2) backfill memory fix + dead-symbol marker guard [s35+s36 — `data.js` carried both, so
committed together], (3) workspace docs. NOT committed (deliberate): `storage/data/_quarantine_grain/`
(8.3M, reversible, NOT gitignored — left untracked), `.antigravitycli/`, repo-local `skills/`,
`scripts/dev/check_hygiene.js`, `backend_correlation_preflight.test.js`.

**Ubuntu machine turned OFF** mid-session → SSH-sync/remote-backfill carryover stays parked.

## Update - 2026-06-15 session 37 — unify rollup to ALL timeframes + custom-TF support (fixes crypto 1w:1)

Boot on HEAD `b2ba6f43`. User ran `backend integrity`, saw every crypto symbol at `1w:1` and "no
difference" after deep backfill, and asked to "roll up for all, just ingest the 1min data … easier to
get custom tf" + use `/mass-implement`. Planned (`~/.claude/plans/sunny-zooming-peacock.md`,
ExitPlanMode-approved with custom-TF added) then implemented in 4 batches.

**Root cause:** the rollup chain was hard-capped at `4h` — deep-backfill/daemon/`intraday-rollup` never
derived `1d`/`1w`/`1mo`. Weekly came only from a manual `ingest --timeframe 1w` nobody ran → `1w:1`.

**Change:** ingest the base grain, derive everything above it.
- `ingest_market_data/constants.js`: `parseTimeframe`/`parseTimeframeMs`/`bucketStartFor` — arbitrary
  `<n><unit>` TFs (`2h`,`6h`,`8h`,`12h`,`3d`…) + **calendar-correct** weekly (Monday) / monthly (1st)
  bucketing (was fixed 7d-from-Thursday / 30d).
- `ingest_market_data/index.js` `aggregateCandles`: buckets via `bucketStartFor`; interval resolves via
  `SUPPORTED_INTERVALS[tf] ?? parseTimeframeMs(tf)`. `deriveHighTfFromLocalDaily` inherits the fix.
- `data.js`: `FULL_TF_ORDER=[1m…1mo]`; `rollupTargetsAboveBase` → whole ladder above base; **two-stage
  `rollupFromBase`** (stage 1 ≤1d from intraday base, windowed via `sinceMs`; stage 2 >1d clean-rebuilt
  from the full small 1d bin → no OOM, no partial weekly/monthly under the daemon's day-aligned window);
  custom TFs routed by parsed interval. `intraday-rollup` accepts any coarser `--timeframes`. Daemon
  unchanged (delegates).
- Quarantined the stray `XAUUSD_1m` single-bar stub → `storage/data/_quarantine_grain/`.

**Verified (live, local, no network):** `intraday-rollup --family {crypto,equities,indices,commodities,fx}`
all 0 errors. BTCUSDT `1w` 1→462, `1mo` 107 (weekly=Monday, monthly=1st). Deep daily preserved (SPY/SPX
`1d` 1998→2026, XAUUSD 2003→2026); SPX `1d:7000` unchanged; `1m`/`5m` untouched. Suite **490/490** (488
pass + 2 pre-existing session-36 git-skip; +2 new rollup tests).

**Changed (UNCOMMITTED) files:** `backend/scripts/data_ops/ingest_market_data/constants.js`,
`backend/scripts/data_ops/ingest_market_data/index.js`, `backend/cli/commands/data/data.js`,
`tests/scripts/tests/intraday_rollup.test.js`, `tests/scripts/tests/backfill_daemon.test.js`, workspace
docs. Data: rebuilt 1w/1mo/1d bins across all families + quarantined XAUUSD_1m stub (gitignored/local).

**Next-session first step:** commit decision (suggested: one `feat(rollup): derive all timeframes from
base grain + custom-TF support` + the workspace docs). Note: `storage/data/ts` is gitignored, so the
rebuilt 1w/1mo live only in the working tree — a fresh clone re-runs `intraday-rollup --family <all>`.
**Behavior to know:** for Yahoo families the most-recent ~60–84 days of `1d` are now re-derived from 5m
on each rollup (deep native history preserved by merge; values match native daily). The intraday DEPTH
inconsistency (Yahoo TFs differ in native depth) is still open — separate network re-fetch.

## Update - 2026-06-16 session 37 (cont.) — DATA-LOSS INCIDENT (cache clear + daemon) + recovery in progress

**Incident:** a cache clear followed by a `backfill-daemon` cycle (run from the TUI) destroyed most deep
history. The daemon's deep scope is far shallower than the actual historical depth: `DEFAULT_DEEP_DAYS`
for Yahoo families was only 59d (5m window), and it never fetched deep daily — so decades of daily
history (built earlier via separate `ingest --timeframe 1d --history-days 7000` runs) were not restored.
Crypto deep also truncated under lane concurrency (SOL→42d, DOGE→~5d). Result: indices/commodities/FX
`1d` dropped from ~7000/5000 to ~59; equities to 2021 only; crypto to 2021 + partials. `storage/data/ts`
is gitignored → no git recovery; api_responses cache was also cleared → no fast replay. **Data is
re-fetchable from free providers (public), just slow.**

**Guard added (code, uncommitted, tested 9/9):** `backend/cli/commands/data/backfill_daemon.js` —
`deepAlsoNeedsDaily(family,mode)`: a DEEP job for any non-crypto family now also runs
`ingest --timeframe 1d --history-days 7300` (so a cleared Yahoo/equity bin restores deep daily, not a
59-day stub that reports "OK"). `DEFAULT_DEEP_DAYS` enlarged (user-edited: crypto 5650, others 7650).
Crypto exempt (native 1m spans listing).

**Recovery in progress (user rule: fetch 1m ONLY, then roll up — no direct big-TF fetch):**
- Crypto 1m→listing: `crypto-deep-backfill --days 5650 --delay-ms 250` (bg, `workspace/recovery_crypto.log`).
- Equity 1m→max: `equity-deep-backfill --days 7300 --chunk-delay-ms 400` (bg, `workspace/recovery_equity.log`;
  Alpaca key OK, 41 symbols, 44 VN/non-US skipped).
- Both fetch only 1m + auto-roll-up to 5m→1mo. **Indices/commodities/FX/VN NOT recovered** (no provider
  1m; their deep daily needs a daily fetch the user has deferred under the "1m-only" rule).
- TUI (PID 56540) terminated (was racing writes → 7 orphan `.tmp`, now cleaned).

**Next-session first step:** verify the two bg jobs finished (BTCUSDT 1m back near listing depth / no 70%
gap; equity 1m to ~2016); then decide on the deferred daily-history fetch for Yahoo families. Commit
decision still pending for ALL session-37 work (rollup-all + custom-TF + daemon guard).

## Update - 2026-06-16 session 37 (cont.) — unified one-command "Deep Backfill" (DEEP_PLAN) + 3 bug fixes

The manual recovery exposed that the daemon's deep path couldn't restore full history. Reworked the TUI
**"Deep Backfill"** (= `backfill-daemon`, manifest.js:162) so ONE run restores every family at finest
grain × max history per the user's model (poll all native TFs at max depth → keep finest → rollup
overwrites recent same-provider bars, deep native preserved — verified via PROVIDER_PRIORITY tie rule).

**Fixes (all UNCOMMITTED on `feat/session-guard-intraday-rollup`):**
- `backend/scripts/data_ops/ingest_market_data/index.js` — **bulk gate bug**: gated on the *base* TF +
  freshness-only, so a deep `--timeframe 1d --history-days N` bulk request was skipped when 5m was fresh.
  Now gates on the *requested* TF AND requires it to cover the requested depth (`readCoverage.firstBarMs
  <= targetStart`). (Per-symbol always bypassed the gate — that's why manual per-symbol worked.)
- `backend/cli/commands/data/data.js` — `commandIngest` now accepts `--provider` (+`--force`) → pins the
  deepest provider for deep daily (Yahoo for eq/indices/commodities, Frankfurter for fx; Alpaca daily
  caps ~2020).
- `backend/cli/commands/data/backfill_daemon.js` — **`DEEP_PLAN`** table (per-family TF acquisition) +
  `runDeepStep`; `makeRealExecutor` runs the full plan PER-SYMBOL on a deep job (crypto: 1m only;
  equities: 1m + Yahoo 1d; indices/commodities/fx: Yahoo 1d + native 1h 730d + 5m-accumulate), then
  rollup. Incremental still tops only the finest. Replaced the `deepAlsoNeedsDaily` guard. Added
  `--deep-all` (force full plan on every live symbol, skip dead) wired to TUI "Full rebuild?".
- `manifest.js` — "Deep Backfill" gets the `--deep-all` confirm.

**Verified:** suite **492/492** (490 pass + 2 pre-existing git-skips; +2 new daemon tests). Live single
FX symbol via `backfill-daemon --once --deep-all --families fx --symbols NZDUSD` → NZDUSD 5m 84d +
1h deep + **1d 5129 from 2006** + 1w/1mo to 2006, 0 errors. Recovery state so far (this session, manual):
crypto 12/18 deep 1m (to 2018); US equity 1m→2020 + rollup; indices/commodities/fx deep daily/weekly/
monthly restored; VN daily restored (VN 5m thin — the deep plan fixes via accumulate). RNDRUSDT delisted.

**To finish the recovery (user action):** run TUI **Deep Backfill** with **Full rebuild? = yes**
(`backfill-daemon --once --deep-all`) — one command now restores crypto 13-18, US-equity deep daily
1998-2020 (Yahoo), VN 5m, and Yahoo-family native 1h to 730d. Multi-hour (crypto). Don't run a second
backfill/TUI action concurrently. Plain `--once` (no deep-all) = cheap incremental maintenance.

### Session 37 close-out (2026-06-16) — COMMITTED + recovery near-complete

**Committed** on `feat/session-guard-intraday-rollup`: `05a10b61` (feat: rollup-to-all + custom TFs +
one-command deep backfill + 3 bug fixes) + `168d6fae` (docs). Suite 492/492.

**Recovery executed this session (via the now-fixed pipeline):**
- US equities: deep daily→1997 (Yahoo bulk, gate-fixed) + 1m→2020 + deep 1w/1mo. All 44 at max depth
  (ARM/COIN at real IPO). Indices/commodities/FX: deep daily/weekly/monthly + native 1h→730d + 5m. VN:
  deep daily/weekly/monthly.
- Crypto: 16/18 deep 1m — BTC/ETH/BNB/SOL/XRP/DOGE/SUI/ADA/LINK/PEPE/WIF/SHIB (earlier) + POL/FET/AVAX/
  NEAR (this session). **INJUSDT still shallow (1m:90k from 2026-03)** — user killed the run before it
  finished. RNDRUSDT delisted (expected).
- Verified post-kill: all bins readable, no corruption, no backfill processes left.

**Only remaining gap:** `INJUSDT` 1m. Finish with one command — TUI **Deep Backfill** (or
`crypto-deep-backfill --symbol INJUSDT --days 5650`). Everything else is at max grain × max history.
The transient `workspace/recovery_*.log` files were cleaned up.

## Update - 2026-06-17 session 38 — FW2 monolith deconstruction (Batches 1 & 2 complete)

Branch `feat/session-guard-intraday-rollup`. Suite 490/490 (2 env-skip).

**Commits landed:**
- `37d54c47` — `refactor(backend,research,trade)`: backend.js→3 children, research.js→2 children, trade.js→2 children (7 new files)
- `91bec9b2` — `refactor(data)`: data.js→3 children: data_rollup.js, data_deep_backfill.js, data_accumulate.js

**Key lesson learned (both batches):** Every child module that is tested via `Module._load` stubs must use LAZY requires (`const { fn } = require(...)` inside the function body, NOT at top level). Top-level destructuring binds at FIRST module load; cached modules retain stale stubs from previous test runs. Applied fix pattern: lazy require inside command function bodies for `ingestMarketData`/`loadConfig` (from ingest) and `printPayload` (from utils).

**Batch 3 (ingest_market_data/index.js) — PAUSED at user request:**
- Attempt was made but reverted: the equity/commodity/FX/crypto provider files cannot be safely extracted without breaking tests that use stub-during-LOAD pattern (stub active only during `require(index.js)`, restored before calling provider functions).
- Extractable (no test breakage): `candle_utils.js` (aggregateCandles), `manifests.js` (FAMILIES_MANIFEST etc.), `providers/prediction.js` (prediction market functions).
- NOT extractable without test changes: `fetchCryptoSnapshot`, `fetchEquityOrIndexSnapshot`, `fetchCommoditySnapshot`, `fetchFxSnapshot` — these are tested with providers stubbed at module-load time only.
- Status: index.js restored to original. Batch 3 deferred to future session.

**Open carryovers:**
- Batch 3 partial split (candle_utils + manifests + prediction extraction) — safe but paused
- Ubuntu SSH / data sync
- FW6 backward-gap fetch
- Merge feat/ml-onnx-section → main (user decision)

## Update - 2026-06-18 session 39 — FW2 Batch 3 (the paused safe partial split) completed + a real bug found+fixed

Boot via `/session-orchestrator`. HEAD was `91bec9b2` (session 38). Found the working tree had
MORE uncommitted changes than any doc described: besides the usual deliberate carryovers
(`.antigravitycli/`, repo-local `skills/`, `check_hygiene.js`, `backend_correlation_preflight.test.js`,
`storage/data/_quarantine_grain/`), 10 tracked files were modified with no handoff entry —
`backend_integrity.js` ("Audit Vintages" mode), `lib/utils.js`, `kronos_integration_test.cpp`,
gateway `cycle.ts`/`index.ts`, `ingest_market_data/constants.js`, `secret_pattern_check.js`,
`seed_master_fixture.js`, `mcp/gate.js`, `config/trading/strategies.yaml`. Asked the user how to
handle that batch; answer was "continue last session work" — interpreted as: don't archaeology the
mystery batch, just resume the documented FW2 Batch-3 carryover.

**Bug found+fixed while investigating (unrelated to FW2):** `config/trading/strategies.yaml` had an
exact-duplicate `registry:` block pasted in. The hand-rolled line-based `readStrategyRegistry()`
reader (`backend/cli/commands/strategy/strategy.js`, no real YAML parser) doesn't reset on a repeated
top-level key, so it silently returned 28 entries — each of the 14 strategy files twice. Confirmed via
direct probe, not caught by any test. Fixed by removing the duplicate block (uncommitted — bundled with
the mystery batch's commit decision, still the user's call).

**FW2 Batch 3 completed in two commits** on `feat/session-guard-intraday-rollup`:
- `3c9aea9a` — extracted `candle_utils.js` (`aggregateCandles`, pure function, zero cross-file
  closures). index.js 2227→2229 lines briefly, then:
- `4e8cf240` — extracted `manifests.js` (`FAMILIES_MANIFEST`/`OPTIONS_MANIFEST` + the still-unimplemented
  provider stubs they reference) and `providers/prediction.js` (Kalshi/Polymarket record builders +
  fetchers, Google Custom Search interest signal). index.js now 1797 lines (was 2227).

**The circular-dependency problem that paused this batch last session is solved via lazy-require
wrappers**, not avoided: `FAMILIES_MANIFEST`'s closures call 4 snapshot fetchers + 2 resolvers that
must stay in index.js (the genuinely NOT-extractable provider-chain functions), so `manifests.js`
defines thin wrappers (`function fetchCryptoSnapshot(...args) { return require('./index.js').fetchCryptoSnapshot(...args); }`)
that lazy-require index.js **at call time** — index.js requires manifests.js at its own top level
(one-way), manifests.js never requires index.js at its top level, so there's no load-time cycle, only
a safe deferred one. Same pattern for `prediction.js`'s one cross-file need (`redactUrl`, which needs
`SENSITIVE_QUERY_PARAMS` that's private to index.js). **Verified this isn't just "loads without
throwing"** — direct probes call through both wrapper boundaries and confirm execution reaches the
real index.js function body (a deliberately-bad-input crypto fetch surfaced the function's own
internal error, not a "not a function" error; a `redactUrl` probe through `predictionCandleRecord`
correctly redacted a `secret=` query param).

**Preserved as-is (not introduced, not fixed):** `fetchEcbHistory` is referenced inside the `fx`/`ecb`
branch of `FAMILIES_MANIFEST` but was never defined or imported anywhere in the original file either —
a pre-existing dangling reference that only throws if that exact branch is hit. Moved verbatim;
behavior unchanged.

Suite **490/490** (2 env-skip unchanged) after both extractions, verified with `npm test` before each
commit. Module.exports keys on `index.js` unchanged; no callers or tests modified.

**Open carryovers (unchanged + new):**
- Ubuntu SSH / data sync, FW6 backward-gap fetch, merge `feat/ml-onnx-section` → main (user decision).

## Update - 2026-06-19 session 39 (cont.) — vintage-audit batch committed + FW2 Batch 4 (the previously-paused part) completed

User said "complete what's left" -> asked which of two things was meant -> "Both of the above":
(1) commit the in-progress concurrent batch, (2) attempt FW2 Batch 4.

**1. Committed the concurrent "vintage audit" / human-readable-CLI-output batch** (`f90d3e28`,
`93d9b1fc`). That work (not mine — a separate live editing session, last touched 4 min before I
checked) turned out to be a coherent, well-formed feature pass: consistent `renderX()` non-JSON
console output for `backend status/stats`, `status`, `quotes status`, `optimize`, `data validate`,
a new `backend integrity --audit-vintages` mode + TUI flag, a real fix to
`shared/lib/market/validation.js` (`validateOhlcv` now branches to a `validatePoint` validator for
point/tick-shaped records instead of demanding open/high/low/close on data that was never OHLC),
and `coverage.js` surfacing `provider`/`derivedFrom` from the meta sidecar. Verified green (490/490)
before committing, split into 2 commits (the coherent feature vs. unrelated misc fixes — gate.js
auth-route gap, kronos test fixture path, seed fixture realism, secret-pattern-check exclusions, TS
import extensions, utils.js require reorder). My earlier `strategies.yaml` dup-registry fix needed no
commit — turned out to be a local-only corruption on top of an already-clean HEAD, so removing it just
restored the file to match HEAD exactly (zero diff).

**2. FW2 Batch 4 completed** (`f26c6533`) — the piece sessions 38 and earlier-this-session explicitly
paused. Extracted the 4 monolithic provider fetchers (`fetchCryptoSnapshot`,
`fetchEquityOrIndexSnapshot`, `fetchCommoditySnapshot`, `fetchFxSnapshot`) + their exclusive helpers
into `snapshot_fetchers.js`. index.js: 2227 -> 1342 lines total across the whole FW2 effort.

**Root cause of the prior block, finally diagnosed:** the existing tests for these 4 functions stub
`shared/lib/providers` via `Module._load` while doing `delete require.cache[ingestPath]; require(ingestPath)`.
That only purges index.js's own cache entry. Once the 4 functions live in a sibling file, the
sibling's top-level provider bindings get captured at *its own* require-time — but since the sibling's
cache entry was never purged, a later test reusing the stale sibling (cached with a different test's
stub, or no stub) gets the wrong provider bindings. Confirmed empirically: naive split -> 8 test
failures, all on the 4 moved functions, first call in each file passing (fresh cache) and every
subsequent call in the same file failing (stale cache) — exactly the predicted shape.

**Fix:** every affected test (`crypto_5m_backfill.test.js`, `equity_5m_backfill.test.js`,
`five_min_fetchers.test.js`) now purges the *whole* `ingest_market_data/` directory tree from
`require.cache`, not just `index.js`'s single entry — covers `candle_utils.js`, `manifests.js`,
`providers/prediction.js`, `snapshot_fetchers.js`, and any future split sibling, generically. 8/8
fixed; verified by running the affected files twice in sequence (ordering flakiness check) and the
full suite (490/490, 2 pre-existing skips). Also verified via direct runtime probes (not just
load-checks) that the lazy-require wrappers for `resolveEquityOrIndexSymbol`/`appendRecords`/
`redactUrl` (kept in index.js, used more broadly there) actually reach the real index.js functions.

**FW2 monolith deconstruction is now fully done** — backend.js/research.js/trade.js/data.js (session
38) + candle_utils.js/manifests.js/providers/prediction.js/snapshot_fetchers.js out of
ingest_market_data/index.js (this session). No further "paused" pieces remain in this initiative.
