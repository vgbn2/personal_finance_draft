# Handoff — 2026-06-13

## Update - 2026-06-13 session 24 (Claude orchestrator) — sessions 23/23b batch verified + committed; 5m crypto+equity CLOSED

**Context:** Boot per session-orchestrator. The harness-tracked background task for the session-22
18-symbol 1825d crypto rerun reported completed (exit 0); sessions 23/23b (run outside Claude) had
implemented the synthetic-5m guard and US-equity 5m Phase 2 but left the ENTIRE batch uncommitted
(19 modified files + 1 new test — 4th occurrence of the "docs say done, git says uncommitted"
drift class). User chose: verify + commit, and verify the Codex equity backfill claims.

**Independent verification (all claims held):**
- Backfill task JSON: all 18 symbols accounted for, 0 errors (13 full-depth; PEPE 326,571 /
  WIF 238,780 / POL 183,534 listing-bounded; RNDRUSDT 969 stale/delisted).
- `readTsIndex` probe over `storage/data/ts/*_5m.bin`: **33 Alpaca-backed equity bins,
  3,101,322 merged `provider=alpaca` rows — exact match** to session 23b's claim (min ARM=51,993,
  max QQQ=112,021). Crypto: 13 long-listed symbols at ≥525,506 bars (5 slightly above — newer
  bars merged on top, so don't probe for "exactly 525,506").
- Full diff reviewed per codex-review workflow: no hallucinated conventions; alpaca.js pagination
  has a monotonic progress guard; per-page push-spreads are bounded (≤10k, under the V8 limit);
  equity-deep-backfill failure accounting matches the session-22 loud-failure semantics.
- Full suite re-run by orchestrator: **395/395 exit 0** (confirmed baseline).

**Commits (4):**
- `a19d6323` feat(data): synthetic 5m experimental-only at the consumer boundary
  (validateSnapshot `synthetic_lower_timeframe`, ml dump default exclusion +
  `--include-experimental-5m`).
- `60458a7a` feat(data): native US-equity 5m Phase 2 — Alpaca paginated
  (`equity-deep-backfill`, 5m→5Min mapping, next_page_token, feed=iex/adjustment=split,
  10k equity chunks, no daily-synthesis fallthrough, provider pinning outside section chains).
- `58130cb9` docs(workspace): sessions 23/23b/24 close-out + runtime artifacts.
- (this docs commit)

**Design note (deliberate):** `ml dump` excludes ALL non-crypto 5m by default — even native
Alpaca equity 5m — until the equity session-gap guard and ML 5m cap/perf gates land.
`isExperimentalSynthetic5mRecord` in `shared/lib/ml/dataset.js` is the single classifier.

**Untracked user artifacts at repo root (NOT committed, user to clean up):**
`personal_finance_draft-state.zip` (136MB), `personal_finance_draft.bundle` (400MB), and
`vgbn1@vgbn-` (400MB, byte-size-identical to the bundle — looks like an scp invocation whose
remote destination became a local filename). Created 2026-06-12 17:53.

**Carryovers (unchanged):** 5m Phase 3 indices/commodities/FX provider/budget decision (or Yahoo
60-day accumulate-forward stop-gap); equity 5m session-gap guard before indicator/backtest
consumption; ML 5m caps/perf gates; NDJSON streaming user sign-off; CLI lazy-requires (optional);
merge `feat/ml-onnx-section` → main = user decision.

## Update - 2026-06-13 session 25 (Claude orchestrator) — 5m Phase 3 all families, daily-truncation fix, Polymarket bulk, mass-backfill coverage

**Plan approved via ExitPlanMode** (Ultraplan cloud handoff failed — repo too large to teleport;
continued locally). Goal: 5m for the remaining asset families + Polymarket historical archive +
max free depth. Plan file: `~/.claude/plans/hidden-exploring-river.md` (has FW1-FW6 follow-ups).

**12 commits on `feat/ml-onnx-section`; suite 422/422 (was 395):**
- `a881ffbe` native Yahoo 5m fetchers (commodity guarded sub-daily branch + `fetchFxSnapshot` +
  `YAHOO_FX_SYMBOLS`).
- `b4edaad2` Polymarket archive hardening (skip-existing resume, index/manifest-v2 merge, opt-in
  429 retry, `--delay-ms`/`--refresh`).
- `f34f594d` `five-min-accumulate` command + 8 commodity ETF proxies (GLD/SLV/CPER/USO/BNO/UNG/
  WEAT/SOYB; CORN excluded — `{symbol}_{tf}.bin` bin collision) + Alpaca SIP clamp (now-16min).
- `dead1fce` validation fix: native sub-daily-sourced 5m no longer rejected at storage (session-23
  guard was stripping every native non-crypto 5m bar — silent zero).
- `37cc190b` TUI manifest surfaces crypto/equity-deep-backfill + five-min-accumulate.
- `7b050f3c` **DAILY-TRUNCATION FIX**: `writeTsIndex` merge-protects ALL timeframes (was replacing
  deep daily/1h/4h bins to the 1 live bar on every ingest; FX survived because its daily also
  lands in JSON). Proven: AAPL daily held at 4822 through 3 subsequent full-index rewrites.
- `474f6bf6` Polymarket: default archive to volume-ordering + fix null `--archive-root` crash.
- `c7893390` Polymarket: paginate past the Gamma 100-row page cap (was archiving ~124 not 2000).
- `d94f8e65` **FW5**: mass-backfill covers `universe_matrix` grid symbols (flat∪grid via
  `massBackfillUniverse`); 92→151 symbols (JPM/GS/AVGO/intl were being skipped).
- + 3 docs commits.

**Wave 0 probes (verified facts):** Alpaca IEX history starts only 2020-07, but **SIP works to
2016-01** (free plan 403s only the last ~15min → SIP clamp). Yahoo 5m via the `range=Nd` form
counts trading days (~84 cal-days, >60d period1/period2 422s) and serves all fx as `PAIR=X`. CLOB
30 calls/1.8s, no 429. **Gamma /markets hard-caps pages at 100** regardless of `limit`.

**Live data landed:** 30-symbol Yahoo 5m accumulate (329,396 bars across indices/commodities/fx);
equity 5m to 2016 via SIP (41/41, AAPL 456k bars); **daily history repopulated DEEP across all
families** (equities 1998-2007, indices 1998, commodities 2003, crypto 2017) + proven durable;
Polymarket bulk archive = 2,045 markets / 82,616 price points / 1,974 feature files (was a
20-market/20-point sample). Crypto 5m re-run to 2017 **STOPPED by user mid-run ("took too long")**
at ~11/18 symbols — BTC/ETH 926k bars (2017-08), BNB/XRP/ADA/LINK/DOGE/SOL also extended; the ~10
remaining alts (PEPE/WIF/SHIB/FET/POL/AVAX/NEAR/INJ/SUI/RNDR) keep their 5y depth (idempotent,
resumable via one `crypto-deep-backfill --days 3300` run — but it's a multi-hour wall-clock job).

**TUI verified end-to-end** via the pipe harness: all three new commands render in the Operational
menu and `five-min-accumulate` dispatches (select/text/confirm flag widgets all work) — dry-run only
(a real run would collide with the ts-index writers).

**Durable traps recorded:** (1) `writeTsIndex` fixed `<bin>.tmp` name → safe within one process
(sync fs serializes on the event loop, so mass-backfill --concurrency N is fine) but two SEPARATE
node processes racing the shared .tmp → EPERM crash; serialize backfill processes (FW1 = per-pid
temp). (2) The daily-truncation replace-semantics bug (now fixed). (3) Gamma 100-row page cap.

**Next-session candidates / open follow-ups (none blocking, all in the plan file):**
- Resume the remaining ~10 crypto alts to their listing dates (flag the multi-hour runtime first).
- FW1 per-pid `writeTsIndex` temp filename (lets deep-backfill + mass-backfill run concurrently).
- FW3 native-poll intraday 15m/30m/1h/4h (user chose native poll over 5m-aggregation; Yahoo 1h=730d).
- FW2 monolith deconstruction (ingest_market_data/index.js ~2000 lines first).
- FW6 backward-gap fetch minimization (lower priority; cache gate + forward-gap already exist).
- Unchanged: equity 5m session-gap guard, ML 5m caps, merge feat/ml-onnx-section → main (user).
- Untracked ~937MB root artifacts (state.zip/.bundle/`vgbn1@vgbn-`) still pending user cleanup.

## Update - 2026-06-13 session 28 (Antigravity orchestrator) — sessions 26-27 batch committed; P3/P4 + FW3 in progress

**Commits:** 5 code commits + 1 workspace commit (55b7869e..77ec6479). Suite 438/438 (new baseline; was 432).

**What landed:**
- docs/workspace reorganization committed (ENOENT deployment contract test fixed)
- Correlation 5m preflight + --drop-non-overlap committed (sessions 26c/26e)
- Mass-backfill integrity report committed (session 26f)
- Hygiene automation + C++ stale-comment purge committed (session 27)
- P3 equity session-gap guard: filterEquitySessionGaps in shared/lib/market/equity_session.js (6/6 tests)
- P4 ML 5m explicit cap: 100k default + --max-rows-5m + VISIBILITY log

**Pre-existing verifications this session:**
- P0 FX integrity: total_stale:0 already green (no ingest needed)
- P1 FW1: atomicTempPath (process.pid+Date.now()+random) already in validation.js:620-623
- P2 MATIC/POL: Option C accepted -- 3-day gap is the MATIC→POL rebrand boundary; use --drop-non-overlap for Layer1 5m

**In-flight subagents:**
- FW3 intraday native poll implementation (data.js, ingest/index.js, manifest.js)
- Crypto alt deep-backfill resumed (crypto-deep-backfill --days 3300, multi-hour)

**Remaining open (none blocking):**
- FW3 result pending subagent report
- merge feat/ml-onnx-section → main = user decision
- ~937MB untracked root artifacts (state.zip/.bundle/vgbn1@vgbn-) pending user cleanup
